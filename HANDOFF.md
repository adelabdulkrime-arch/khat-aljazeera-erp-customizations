# خلاصة مشروع: نظام ERP لورشة وقطع غيار "خط الجزيرة"
> ملف تسليم (Handoff) — الصقه في بداية أي جلسة Claude جديدة ليفهم المشروع بالكامل.
> آخر تحديث: 2026-07-17

---

## 1. ما هو المشروع؟

نظام ERP متكامل لإدارة **ورشة صيانة سيارات + محل قطع غيار**، مبني على **ERPNext / Frappe**، مُعرّب بالكامل (RTL عربي)، ويحاكي منافساً اسمه **Mazoon ERP**.
- اسم الشركة داخل النظام: **خط الجزيرة** (Khat Al Jazeera).
- يخدم عملاء في **عُمان (رمز 968)** و**اليمن (رمز 967)**.
- بريد المستخدم: zayed@fasttera.nl

---

## 2. البيئة والتشغيل (مهم جداً)

- **Frappe/ERPNext v16** عبر **Docker Compose** على Docker Desktop (Windows 11).
- مجلد المشروع: `C:\Users\lenovo-Thinkpad\Desktop\frappe_docker`
- ملف الكومبوز اسمه **`compose.yaml`** (وليس docker-compose.yml).
- الموقع: **erp.local** — الدخول: **Administrator**، وكلمة المرور هي قيمة `ADMIN_PASSWORD` في متغيّرات البيئة (Coolify، أو ملف `.env` محلياً). لا تُكتب هنا.
- الرابط في المتصفح: **http://localhost:8080/app** (أو `/desk`)

### أوامر التشغيل:
```bash
cd "C:\Users\lenovo-Thinkpad\Desktop\frappe_docker"
docker compose ps                     # حالة الحاويات
docker compose up -d                  # تشغيل
```
- ⚠️ **تعارض معروف:** يوجد **WireGuard VPN** (`WireGuardTunnel$mahfood`) يبطّئ/يعطّل شبكة WSL2 أحياناً ويسبب أخطاء 502 أو توقف Docker. الحل: إيقاف الـ VPN، وأحياناً إعادة تشغيل الجهاز بالكامل.
- إذا كان Docker Desktop متوقفاً: شغّله عبر
  `Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"` وانتظر ~2-4 دقائق.

---

## 3. نمط النشر المُتّبع (الطريقة القياسية لكل تعديل)

كل التخصيصات تُكتب كملفات `.py` محلياً في مجلد `frappe_docker`، ثم:
```bash
cd "C:\Users\lenovo-Thinkpad\Desktop\frappe_docker"
MSYS_NO_PATHCONV=1 docker compose cp ملف.py backend:/home/frappe/frappe-bench/apps/frappe/frappe/ملف.py
MSYS_NO_PATHCONV=1 docker compose exec backend bench --site erp.local execute frappe.ملف.execute
```
- **`MSYS_NO_PATHCONV=1`** ضروري قبل أوامر docker على git-bash في ويندوز (وإلا يفسد المسارات).
- استخدم أداة **Bash** (وليس PowerShell) لأوامر docker.

---

## 4. الملفات الرئيسية للتخصيص (كلها في `frappe_docker`)

| الملف | الوظيفة |
|------|---------|
| `workshop_futuristic.py` | **وحدة مشتركة** (CSS/JS) تستوردها كل اللوحات السبع. تعديلها يتطلب: (أ) `docker compose restart backend websocket queue-short queue-long`، و(ب) **إعادة تنفيذ كل اللوحات السبع** لأن الـ JS يُخبز داخل كل Custom HTML Block عند التنفيذ. |
| `workshop_home.py` | لوحة الرئيسية + مربع البحث بالجوال/رقم اللوحة |
| `workshop_dashboard.py` | لوحة "إدارة ورشة الصيانة" (18 بلاطة + بطاقات إحصائية + زر طي الإحصائيات) |
| `workshop_accounting.py` / `workshop_inventory.py` / `workshop_purchasing.py` / `workshop_sales.py` / `workshop_general_settings.py` | باقي اللوحات |
| `workshop_scripts.py` | Client/Server Scripts: حسابات المجاميع، أزرار واتساب، تعبئة بيانات العميل العائد، أيقونة واتساب في القوائم |
| `workshop_gl_stock_integration.py` | ربط بطاقة العمل بحركة المخزون الحقيقية، وفاتورة الإصلاح بـ Sales Invoice، ودفعات الورشة بـ Payment Entry |
| `workshop_invoice_whatsapp.py` | توليد PDF للفاتورة وإرجاع رابطها لإرساله عبر واتساب |
| `workshop_translations.py` | جداول الترجمة العربية |

---

## 5. المزايا المُنجزة (Completed)

1. **تصميم كامل مُعرّب** — أيقونات SVG، طبقة تصميم مستقبلية، صفحة دخول مخصصة، لوحة أوامر (Ctrl+/)، مبدّل لغة في شريط التنقل.
2. **ربط محاسبي/مخزوني حقيقي** — بطاقة العمل "جاهزة للتسليم" ⟵ Stock Entry حقيقي؛ فاتورة الإصلاح ⟵ Sales Invoice حقيقية؛ دفعات الورشة ⟵ Payment Entry حقيقي (مع معالجة الدفع الزائد والحد الأدنى).
3. **بحث سريع في الرئيسية** بالجوال أو رقم لوحة المركبة → يعرض ملف العميل كاملاً.
4. **أزرار واتساب:**
   - على بطاقة العميل: "تواصل عبر واتساب".
   - على فاتورة الإصلاح: "إرسال عبر واتساب" — يُولّد **PDF فعلي للفاتورة** ويضع رابطه في الرسالة (عبر `wa.me`).
   - **أيقونة واتساب خضراء بجانب رقم الهاتف داخل قائمة "مركبات العملاء"** (عمود العميل).
   - منطق استنتاج رمز الدولة: 8 أرقام → 968 (عُمان)، 9 أرقام تبدأ بـ 7 → 967 (اليمن).
5. **تعبئة تلقائية للعميل العائد** — عند اختيار عميل في بطاقة العمل، تُعبّأ بياناته ومركباته تلقائياً (مع نافذة اختيار لو عنده أكثر من مركبة).
6. **زر طي/عرض الإحصائيات** في لوحة الورشة (مثل Mazoon "إخفاء الإحصائيات") — يحفظ الحالة في localStorage.
7. **نسخ احتياطي** — تلقائي مجدول + يدوي. النسخ اليدوية تُنسخ إلى `C:\Users\lenovo-Thinkpad\Desktop\erp-backups\`.
8. **كتالوج عمليات الاستبدال** (2026-08-24, بناءً على تسجيلين صوتيين من المالك) — 14 صنف "تبديل..." محدد بالاسم (بطارية، فلتر زيت، فلتر هواء، بوش، بلية، لينك رود، رود إند، راك إند، سير محرك...) تحت مجموعة صنف "عمليات الاستبدال" (`workshop_replacement_operations.py`)، ليختارها الموظف كسطر منفصل عند إنشاء عرض السعر/فاتورة المبيعات بدل رقم إجمالي واحد. **الأسعار = صفر افتراضياً، يحتاج المالك يعبّئها بنفسه.**
9. **قالب طباعة مفصّل** (`workshop_operations_print_format.py`) لفاتورة المبيعات وعرض السعر — يعرض رقم اللوحة، رقم الشاصي (VIN)، التاريخ، الساعة، وجدول الأعمال المنفذة بنداً بنداً + الإجمالي. **غير مفعّل كقالب افتراضي** — يُختار يدوياً من قائمة "طباعة" حتى يقرر المالك اعتماده.
10. **ترميز وترقيم مطابق لمازون** (2026-08-25, بعد ما شارك المالك سكرين شوت حقيقي من نظام مازون) —
    - أكواد كتالوج العمليات صارت `SRV-####` مولّدة تلقائياً (مو `WS-OP-001` يدوي) عبر `frappe.model.naming.make_autoname`، **يستحيل تكرارها** لأنها نفس آلية Naming Series الذرية بالإطار نفسه. القطع الحقيقية (بطاريات بأحجامها...) لما تُضاف لاحقاً، تتبع نفس المنطق بادئة `PRD-.####`.
    - `workshop_document_numbering.py`: أضاف تسلسل `WI-.YYYY.-` (فاتورة) و`WQ-.YYYY.-` (عرض سعر) لمستندات الورشة، يُختار تلقائياً بمجرد ربط حقل `vehicle` بمستند جديد — بيع نقطة بيع عادي بدون مركبة يبقى على التسلسل الافتراضي. **بدون دولايتايب منفصل** (نفس Sales Invoice/Quotation الحقيقيين) — القرار المتعمد بعدم إعادة "Repair Invoice" لسا قائم.
    - **لسا ناقص:** لقطة فعلية لمستند "فاتورة الإصلاح WI-xxxx" نفسه من مازون (عندنا بس عرض السعر WQ وسند القبض اللي أشار له). لو توفرت لاحقاً، تُراجع مطابقة تصميم `workshop_operations_print_format.py` معها بالتفصيل.

---

## 6. أمور تقنية دقيقة (Gotchas) اكتشفناها

- **Server Script sandbox (safe_exec):**
  - `frappe.defaults.get_global_default` غير متاح → استخدم `frappe.db.get_single_value("Global Defaults", "default_company")`.
  - دالة `.format()` محظورة → استخدم تنسيق `%`.
- **Client Script** له حقل `view` بقيمة `"Form"` أو `"List"` (الدالة `_client_script()` تقبل معامل `view`).
- **البحث العام (Ctrl+G):** يحتاج `in_global_search=1` على الحقل (عبر Property Setter)، ويُزامَن عبر مهمة مجدولة **كل 15 دقيقة** (ليس فورياً). أما بحث الرئيسية فيستعلم مباشرة من الجداول (فوري).
- **Quick Entry dialogs** ليس لها `frm` — الـ Client Scripts العادية لا تعمل داخلها؛ يُستخدم مستمع DOM عام (blur في طور الالتقاط).
- **توليد PDF (wkhtmltopdf):** يجب ضبط `host_name = "http://frontend:8080"` مؤقتاً (حاوية nginx تخدم الأصول الساكنة، بينما gunicorn على :8000 لا يفعل).
- **رابط PDF العام:** حالياً `http://localhost:8080` — **لن يعمل على هاتف العميل** إلا بعد نشر النظام على دومين حقيقي. يوجد حقل جديد في "إعدادات الورشة" اسمه **"الرابط العام للنظام"** يُملأ عند النشر.
- **بعد restart للـ backend:** غالباً تحتاج `docker compose restart frontend` أيضاً (nginx يخزّن IP قديم → 502).

---

## 7. المهام المعلّقة / تحتاج متابعة

1. **الفاتورة اليتيمة `RINV-2026-00001`** — عميلها "عميل اختبار بحث الجوال" محذوف، لذا بلا رقم جوال. يحتاج إما ربطها بعميل حقيقي أو حذفها.
2. **تكرار منطق رمز الدولة** موجود في 3 أماكن (`CUSTOMER_JS`, `INVOICE_JS`, وأيقونة القائمة) — يُفضّل توحيده في دالة مشتركة لاحقاً.
3. **النشر الإنتاجي** — HTTPS جُهّز لكن لم يُفعّل بالقوة؛ يحتاج خادم/دومين عام حقيقي ليعمل كل شيء (خاصة روابط واتساب).
4. **الإرسال التلقائي الكامل عبر واتساب** (بدون ضغط زر) — يتطلب حساب **WhatsApp Business API** رسمي من Meta أو خدمة مثل Twilio (المستخدم يجب أن ينشئه بنفسه). الحل الحالي مجاني ويعتمد على `wa.me`.
5. ~~زر "إرسال عبر واتساب" على الفاتورة معطّل~~ **✅ تم إصلاحه 2026-08-25.** كان `workshop_scripts.py` و`workshop_invoice_whatsapp.py` يستهدفان الدولايتايب الوهمي "Repair Invoice" المحذوف. أُعيد توجيه الاثنين لـ Sales Invoice الحقيقية (`SALES_INVOICE_JS` بدل `INVOICE_JS`)، ويستخدم `workshop_invoice_whatsapp.py` الآن قالب الطباعة المفصّل الجديد صراحةً (`PRINT_FORMAT` بالملف) بدل قالب Sales Invoice الافتراضي. نفس الشي لـ `PAYMENT_SERVER` (كان يستهدف "Workshop Payment" المحذوفة أيضاً) — أُزيل بالكامل، لأن Payment Entry الحقيقي في ERPNext يغطي نفس الوظيفة أصلاً (paid_amount/outstanding/status تلقائياً، مع معالجة إلغاء صحيحة ما كانت موجودة بالنسخة اليدوية).
   أثناء المسح لقيت **بق ثالث من نفس العائلة**: `workshop_home.py` (بروفايل العميل بالصفحة الرئيسية) كان يستعلم `Repair Invoice` حيّاً لعرض إجمالي الفواتير/المدفوع/المتبقي لكل عميل — أُصلح بنفس المنطق (Sales Invoice، `outstanding_amount`، والمدفوع يُحسب `grand_total - outstanding_amount` بدل `paid_amount` لأن الحقل الأخير ما يُعبّى إلا لفواتير نقطة البيع). **راجعت باقي الملفات** (`workshop_dashboard.py`, `workshop_translations.py`, `workshop_oman_setup2.py`, `workshop_gl_stock_integration.py`) — الإشارات المتبقية لـ"Repair Invoice" فيها كلها تعليقات توثيقية/تاريخية أو نصوص ترجمة غير مستخدمة، مو كود حي.
6. **تسعير عمليات الاستبدال الجديدة** — الأصناف الـ14 المُضافة في `workshop_replacement_operations.py` أسعارها صفر، يحتاج المالك يحدد سعر كل عملية (عمل + قطعة أو عمل فقط) قبل الاستخدام الفعلي.
7. **✅ الفاتورة/عرض السعر الآلي من بطاقة العمل (2026-08-25)** — `workshop_work_card_to_invoice.py` (دالتان whitelist، بدون STEPS): زر "إنشاء فاتورة" (يظهر بعد اعتماد بطاقة العمل فقط — نفس لحظة خصم القطع من المخزون) وزر "إنشاء عرض سعر" (بدون قيد الاعتماد) على نموذج بطاقة العمل، يبنيان Sales Invoice/Quotation حقيقية عبر `doc.insert()` (مو نسخ حقول يدوي بجافاسكربت زي النظام القديم المتراجع عنه) بترقيم `WI-`/`WQ-`. اختُبر: تعيين الأصناف صحيح (خدمة+قطعة)، رفض الفاتورة قبل الاعتماد، عدم تكرار الفاتورة عند الضغط مرتين على نفس البطاقة، عرض السعر يعمل بدون اعتماد.
8. **🔬 تحقق حقيقي من الكود (2026-08-25)** — طُلب التأكد إن كل شي "يشتغل حقيقة لا افتراضياً". حاولت تشغيل بيئة Frappe/ERPNext حية كاملة (MariaDB+Redis+bench) داخل بيئة الجلسة نفسها للاختبار المباشر — اصطدمت بقيود بنيوية بالبيئة (`bench` يرفض العمل كـroot، وإنشاء مستخدم غير-root تسبب بـtimeout)، فتحوّلت لطريقة أخف لكن حقيقية فعلاً: **استنسخت الكود المصدري الحقيقي لـFrappe وERPNext من GitHub** (نسخة `version-15`) وقارنت كل API استُخدم بكودنا الجديد مقابل التطبيق الفعلي — مو افتراض من الذاكرة. النتيجة:
   - **لُقي خلل حقيقي وأُصلح:** `frappe.make_property_setter()` بـ`workshop_document_numbering.py` كانت تُستدعى بقاموس واحد (`{...}`) — التوقيع الحقيقي بالإطار يأخذ معاملات منفصلة بالترتيب (`doctype, fieldname, property, value, property_type`). كانت ستفشل بخطأ `TypeError` أول مرة تُنفَّذ هذي الخطوة فعلياً. تم الإصلاح والتحقق من التوقيع الصحيح مباشرة من `frappe/custom/doctype/property_setter/property_setter.py`.
   - **تحقق ونجح:** آلية `make_autoname("SRV-.####")` (تُنتج فعلاً `SRV-0001` بالتحليل الحقيقي لقواعد الترميز بـ`frappe/model/naming.py`)، آلية `naming_series` التلقائية على Sales Invoice/Quotation الحقيقيين (تأكدت `autoname: "naming_series:"` وقيم `options` الافتراضية فعلياً من `erpnext.git`)، أسماء كل الحقول المستخدمة (`customer`, `customer_name`, `outstanding_amount`, `item_code`, `qty`, `rate`, `apply_discount_on`, `discount_amount`...) على Sales Invoice/Quotation/Sales Invoice Item/Quotation Item، وحقول دولايتايب `Print Format` و`Client Script` المستخدمة بالملفات الجديدة — كلها طابقت المصدر الحقيقي تماماً.
   - **لسا غير مُتحقَّق منه حياً:** تنفيذ فعلي كامل (`bench migrate` على قاعدة بيانات حقيقية) وتوليد PDF فعلي بـwkhtmltopdf لزر الواتساب — يحتاج بيئة Docker كاملة (frontend/backend/db حقيقية) ما توفرت بالجلسة. يُنصح تشغيل `docker compose up -d --build` على بيئة تجربة فعلية قبل الإنتاج، ومراقبة `docker compose logs -f init` (راجع QUICKSTART.md المحدّث).
9. **📝 QUICKSTART.md كان قديم/خاطئ — تم تحديثه بالكامل (2026-08-25).** كان يوثّق طريقة نشر قديمة (نسخ ملفات `.py` منفردة داخل `apps/frappe/frappe/` عبر `docker compose cp` + `bench execute frappe.X.execute`) — تجاوزها المشروع لما صار `khat_workshop` تطبيق Frappe حقيقي (Dockerfile + `hooks.py` + `after_migrate`). الدليل الجديد يغطي مسارين: **Coolify** (الأنسب — `docker-compose.yml` مبني له تحديداً حسب تعليقات الملف نفسه) و**VPS عادي**، بالاعتماد فقط على الملفات الحقيقية الموجودة (`Dockerfile`, `docker-compose.yml`, `docker/init.sh`, `.env.example`).

---

## 8. مبادئ العمل المُتّبعة

- **قاعدة صارمة:** لأي ميزة جديدة → أنشئ بيانات اختبار → تحقّق في المتصفح → **احذف بيانات الاختبار فوراً**. لا تترك أي بيانات ملفّقة في النظام الحي.
- عند اختبار واتساب: **لا تضغط "إرسال" أبداً** — المتصفح فيه جلسة واتساب ويب الشخصية الحقيقية للمستخدم. اكتفِ بالتحقق من الرابط ثم أغلق التبويب.
- المرجع المرئي الحي لـ Mazoon: **alamal.mazoonerp.com** (التينانت الأصلي alaseel معطّل).

---

## 9. مواقع أخرى للمستخدم (منفصلة — لا تخلط بينها)

- **AATC** (Botble/Laravel) — مشروع مختلف تماماً.
- **sintkaras.sprig-ye.cloud** — موقع Botble حي منفصل.
- **pricing-site** (Botble) — على المنفذ 8001.
- راجع ملفات الذاكرة في `.claude/.../memory/` لتفاصيل كل منها.
