# خطة المعالجة ومتابعة التقدم — نظام خط الجزيرة ERP

> **الغرض من هذا الملف:** مصدر واحد للحقيقة. كل ما تم تشخيصه موثّق هنا بمراجع دقيقة
> (ملف + رقم سطر) حتى لا نعيد اكتشافه، وكل عمل مُصنّف إلى **مرحلة الديمو** أو
> **إعادة البناء** حتى لا نتشتت بينهما.
>
> **آخر تحديث:** 2026-07-21 — بعد التحقق المباشر من الخادم عبر SSH

---

## ١. السياق والقرار الاستراتيجي

النظام **عُرض على العميل سابقًا عبر ديمو**. القرار الحالي:

| المرحلة | الهدف | المبدأ الحاكم |
|---|---|---|
| **المرحلة أ — الديمو** (الآن) | معالجة ما يراه العميل ويؤثر على قراره | أقل تدخل، أسرع أثر ظاهر. **لا إعادة هيكلة** |
| **المرحلة ب — إعادة البناء** (لاحقًا) | بناء النظام بالشكل الصحيح معماريًا | يُنفَّذ بعد موافقة العميل |

> ⚠️ **قاعدة منع التشتت:** أي بند في المرحلة ب **ممنوع تنفيذه الآن** مهما بدا مغريًا.

### 🔑 معطى حاسم: النظام خالٍ من البيانات

تحقُّق مباشر من قاعدة البيانات: **صفر فاتورة إصلاح، وصفر فاتورة مبيعات.**
النظام حاليًا **إعدادات فقط بلا أي معاملات**. أثر ذلك:

- عيوب الجسر المحاسبي **لم تُنتج بيانات خاطئة بعد** — أُصلحت قبل أول إدخال.
- **لا توجد مخاطر ترحيل بيانات إطلاقًا** → هذه هي اللحظة المثالية والأرخص لتنفيذ
  المرحلة ب. كل يوم إدخال بيانات فعلي يرفع تكلفتها.

---

## ٢. مفتاح الحالات

| الرمز | المعنى |
|---|---|
| ⬜ | لم يبدأ |
| 🟡 | الكود جاهز — بانتظار نشر/تحقق |
| ✅ | مكتمل ومُتحقَّق منه فعليًا على الخادم |
| 🔵 | مؤجل للمرحلة ب (قرار مقصود) |
| 👤 | يتطلب إجراءً بشريًا (لا يمكن أتمتته) |

---

## ٣. المرحلة أ — أولويات الديمو

### ✅ P0 — حاجب الإقلاع: `ERR_CONNECTION_RESET` — **مُغلق ومُتحقَّق منه**

- **السبب الجذري:** حاوية `frontend` كانت على **٣ شبكات**، إحداها (`_default` / 172.20)
  **لا يملك Traefik أي مسار إليها**، ولا يوجد وسم `traefik.docker.network` للترجيح
  → اختيار عشوائي للـ IP عند كل إعادة نشر. **هذا يفسّر «نجح فترة ثم توقف».**
- **الإصلاح:** حذف كتلة `networks` اليدوية من `docker-compose.yml` (commit `f9626af`).
- **التحقق النهائي (2026-07-21):**
  - الواجهة الآن على شبكة **واحدة فقط**: `htgwel6r9845eze7wietga2e → 172.19.0.11`
  - `coolify-proxy` يشترك معها في تلك الشبكة بالضبط
  - **`HTTP 200` خلال 0.4 ثانية** ✅
  - كل الحاويات تعمل، و`init` و`configurator` انتهيا بـ `Exited (0)`

---

### ✅ P1 — الشركة ومعالج الإعداد — **كان منجزًا مسبقًا**

> ⚠️ **تصحيح:** التشخيص الأولي افترض عدم وجود شركة. الفحص المباشر أثبت العكس.

| المفتاح | القيمة الفعلية |
|---|---|
| `Company` | **Khat Al Jazeera** |
| `setup_complete` | `1` |
| `default_company` | Khat Al Jazeera |
| `default_currency` | OMR |

---

### ✅ P2 — تهيئة عُمان وضريبة 5% — **كان منجزًا مسبقًا**

قالب `ضريبة القيمة المضافة عمان 5% - KAJ` موجود و`is_default = 1`،
وحسابه `ضريبة القيمة المضافة - KAJ` بنسبة 5%.
كما تأكد إنشاء **20** DocType مخصصًا و**7** حالات بطاقة عمل.

---

### ✅ P7 — خمس لوحات مفقودة — **أخطر ما اكتُشف، وأُصلح**

> لم يكن هذا البند في الخطة الأصلية — ظهر أثناء التحقق من الخادم.

**العرض:** الشريط الجانبي كان يعرض `Home` و`General Settings` فقط. أما لوحات
**المحاسبة والمخزون والمشتريات والمبيعات والورشة** فكانت **غير موجودة إطلاقًا** —
أي أن العميل كان سيرى نظامًا شبه فارغ.

**الأسباب الجذرية (من سجل `init` على الخادم):**

| الوحدة | سبب الفشل الفعلي |
|---|---|
| `workshop_accounting` | `LinkValidationError: Could not find Row #3: Role: محاسب` |
| `workshop_inventory` | الدور `مدير المستودع` غير موجود |
| `workshop_purchasing` | الدور `مسؤول المشتريات` غير موجود |
| `workshop_sales` | الدوران `مندوب مبيعات` و`كاشير` غير موجودان |
| `workshop_dashboard` | 4 تقارير غير موجودة (الفنيين، العمولات، الإيرادات، بطاقات العمل) |
| `workshop_futuristic` | مكتبة مشتركة بلا `execute()` — كانت مُدرجة خطأً في قائمة التشغيل |

الكتل تُسند أدوارًا عربية في جدول `roles`، و`workshop_setup` كان ينشئ
`Workshop Manager` فقط → فشل الإدراج → **اختفت اللوحات صامتةً في كل نشر**.

**الإصلاحات:**
1. إنشاء كل الأدوار المطلوبة مسبقًا في [`workshop_setup.py`](workshop_setup.py)
2. تصفية روابط التقارير إلى الموجود فعليًا في [`workshop_dashboard.py`](workshop_dashboard.py)
   — فقدان 4 اختصارات أهون من فقدان اللوحة بأكملها
3. نقل `workshop_futuristic` إلى قائمة `LIBS` في [`init.sh`](docker/init.sh):
   **تُنسخ ولا تُنفَّذ** (النسخ ضروري وإلا انكسر الاستيراد في اللوحات)

**التحقق النهائي:** الوحدات الأربع عشرة كلها `OK` و**صفر تحذيرات لأول مرة**،
واللوحات السبع موجودة، والأدوار الخمسة أُنشئت.

---

### ✅ P3أ — الفواتير كانت تُرحَّل بلا ضريبة — **أُصلح ومُتحقَّق منه**

- **الخطورة:** 🔴 مخالفة ضريبية، وليست عيبًا بصريًا.
- **السبب:** `Sales Invoice` المُولَّد تلقائيًا كان يُنشأ **بدون حقل `taxes` إطلاقًا**
  → قالب ضريبة 5% لا يُطبَّق أبدًا.
- **الإصلاح:** [`workshop_gl_stock_integration.py`](workshop_gl_stock_integration.py) —
  جلب القالب الافتراضي للشركة ونسخ صفوفه إلى الفاتورة (`taxes_and_charges` + `taxes`).
- **تحقُّق مسبق:** يوجد قالب افتراضي **واحد فقط** (`is_default=1`) وهو قالب عُمان،
  إذًا الاستعلام حتمي ولا التباس فيه.

---

### 🔵 P3ب — انهيار بنود الفاتورة إلى صنف واحد — **مؤجل للمرحلة ب (بقرار)**

- كل سطر يتحول إلى `"item_code": "WORKSHOP-SERVICE"`.
- **سبب التأجيل — ليس كسلًا بل قيد بنيوي:** فحص `Repair Invoice Item` أثبت أنه
  **لا يحتوي على أي حقل ربط بصنف حقيقي** (حقوله: وصف نصي، كمية، سعر، إجمالي فقط).
  أي أنه **لا يوجد صنف حقيقي أصلًا ليُستخدم**. الإصلاح الصحيح هو إلغاء
  `Repair Invoice` والانتقال إلى `Sales Invoice` مباشرة (بند **ب-٢**) — وأي حل وسط
  الآن سيُرمى لاحقًا.
- **الأثر المقبول مؤقتًا:** الفاتورة المطبوعة **تعرض الوصف العربي الصحيح لكل سطر**
  (لأن `item_name`/`description` تُنسخ من السطر)، فالضرر منحصر في تقارير
  المخزون والربحية على مستوى الصنف — وهي خارج نطاق الديمو.

---

### ✅ P4 — الشعار — **أُصلح ومُتحقَّق منه** + 👤 يتطلب رفع الشعار

- **السبب:** [`workshop_home.py`](workshop_home.py) كان يثبّت `/files/kaj-logo1fe402.png`
  في الكود. اللاحقة `1fe402` بصمة يولّدها Frappe عند الرفع اليدوي → الملف كان على
  جهاز التطوير فقط.
- **تحقُّق قاطع:** مجلد `sites/<site>/public/files/` على الخادم **فارغ تمامًا**.
- **الإصلاح:** إزالة المسار المثبَّت؛ الشعار يُقرأ الآن من Website Settings،
  وإن لم يوجد **يُخفى العنصر** بدل عرض صورة مكسورة.
- 👤 **مطلوب منك:** رفع الشعار وضبطه في `Website Settings → app_logo` فيظهر تلقائيًا.

---

### ✅ P5 — صفحتا «المستخدمون» و«الأدوار» — **أُصلح ومُتحقَّق منه**

- **السبب:** البلاطتان كانتا تشيران إلى `roles-dashboard` و `users-dashboard`،
  و**لا شيء ينشئهما** (تأكد من الخادم: كلتاهما `MISSING`).
- **الإصلاح:** توجيههما إلى قائمتَي `Role` و `User` المدمجتين (تعمل بكفاءة تامة)
  في [`workshop_general_settings.py`](workshop_general_settings.py)
  و[`workshop_futuristic.py`](workshop_futuristic.py) (لوحة الأوامر).
- 🔵 بناء لوحتين مخصصتين → المرحلة ب.

---

### ❌ P6 — حقل «الموظف» — **تشخيص خاطئ، لا توجد مشكلة أصلًا**

> ⚠️ **تصحيح صريح لخطأ ارتكبته:** ادّعيتُ أن `Employee` يعيش في تطبيق `hrms`
> وأن الحقل مكسور. **هذا غير صحيح.**

**ما أثبته الخادم:** `Employee` موجود ويأتي مع **ERPNext نواةً**، ضمن الوحدة
`Setup` — وليس ضمن `hrms`. إذًا الحقل **يعمل بشكل سليم تمامًا** ولم يكن معطلًا قط.

**الإجراء:** حُذف الـ Property Setter الذي أضفته في الدفعة السابقة. كان كودًا ميتًا
مبنيًا على فرضية باطلة، وتعليقه كان مضللًا لمن يقرأ الكود لاحقًا.

**ما يبقى صحيحًا:** تطبيق `hrms` فعلًا غير مثبَّت، فوحدات **الرواتب والحضور
والإجازات وتقييم الأداء** غائبة — لكن هذا لا علاقة له بحقل الموظف (بند **ب-٤**).

---

## ٤. ملاحظات مكتشفة أثناء الفحص (غير حاجبة)

| الملاحظة | التفصيل | الإجراء |
|---|---|---|
| قالب ضريبي غريب | `Yemen Tax - KAJ` (حساب `VAT - KAJ`، 5%) — بقايا إعداد سابق | غير ضار حاليًا لأن `is_default = 0`؛ **يُقترح حذفه** بعد موافقتك |
| Server Scripts غير موثّقة | `Update Invoice On Payment` و `Update Invoice On Payment Delete` **ليسا في المستودع** — أُنشئا يدويًا عبر الواجهة | خطر: سيُفقدان عند أي إعادة بناء. يجب توثيقهما في الكود |

> ⚠️ الملاحظة الثانية مهمة: وجود منطق حيّ خارج المستودع يعني أن **المستودع ليس
> المصدر الكامل للحقيقة** حاليًا. يجب معالجته ضمن المرحلة ب.

---

## ٥. المرحلة ب — إعادة البناء الصحيح

> **الحالة:** بدأ التنفيذ في 2026-07-22 بعد اقتناع العميل وتفويض كامل.

### ✅ ب-٠ — النسخ الاحتياطية الآلية — **مُنجز ومُتحقَّق منه** 🔴 لم يكن في الخطة

اكتُشف أثناء فحص ما قبل التنفيذ: **النظام كان بلا أي نسخة احتياطية إطلاقًا.**

- مجلد النسخ كان فارغًا · لا مهام `cron` على المضيف
- المهمة المجدولة الوحيدة `delete_downloadable_backups` — **تحذف ولا تنشئ**
- **السبب:** في Frappe العادي تكون النسخ التلقائية مهمة `cron` على **المضيف**،
  وهذا المضيف غير موجود داخل الحاويات → نشر Docker بلا نسخ صامتة

**الحل:** خدمة `backup` مخصصة في `docker-compose.yml` تعمل بفاصل زمني قابل
للضبط (`BACKUP_INTERVAL_SECONDS`) مع مدة احتفاظ (`BACKUP_RETENTION_DAYS`).

**قرار مهم:** النسخ تُكتب في **volume منفصل** (`backups`) لا في `sites`، حتى لا
يؤدي تلف بيانات الموقع إلى ضياع النسخ معها.

**مصيدة واجهتها:** أول نسخة فشلت برسالة Frappe المضلِّلة «قاعدة البيانات قد تكون
تالفة». السبب الحقيقي **صلاحيات**: Docker ينشئ نقطة الـ volume مملوكة لـ `root`
بينما الحاوية تعمل كـ `frappe`. عولج بإنشاء `/backups` في `Dockerfile` مملوكًا
لـ `frappe` + تصحيح ملكية الـ volume القائم.

**التحقق:** 12 ملفًا · 8.9MB · آخر دورة `[backup] OK`.

⚠️ **متبقٍّ:** النسخ ما زالت على **نفس الخادم**. النسخ خارج الخادم (S3) يحتاج
مفاتيح تخزين منك.

---

### ✅ ب-٣ — توحيد الصورة على كل الخدمات — **مُنجز ومُتحقَّق منه**

كانت `init` وحدها تُبنى من `Dockerfile`، وبقية الخدمات تستخدم `frappe/erpnext`
الجاهزة → أي شيء يُخبز في الصورة **يراه `init` فقط** ولا يراه `backend` الذي
يُشغّل النظام فعلًا. كان هذا **الحاجب التقني** أمام أي تطبيق مخصص أو `hrms`.

**الحل:** مرساة YAML واحدة (`x-frappe-image`) تُطبَّق على **9 خدمات Frappe**.
قاعدة البيانات و Redis لم تُمَس. الطبقات مشتركة فالكلفة على الذاكرة المؤقتة لا القرص.

**التحقق:** 7 خدمات تعمل على صورتنا · `/backups` موجود داخل `backend` (دليل قاطع).

> 🔴 **درس من انحدار تسبّبتُ به:** بعد التوحيد ظهر **502**. السبب أن `Dockerfile`
> كان ينتهي بـ `CMD ["/opt/init.sh"]`، فورث `backend` هذا الأمر وشغّل سكربت
> التهيئة **بدل gunicorn**، فبقي nginx بلا upstream. كان الأمر غير ضار ما دامت
> `init` وحدها تستخدم الصورة. **العبرة:** توحيد الصور يكشف كل افتراض ضمني في
> `CMD`/`ENTRYPOINT`. عولج بحذف التجاوز ووراثة `CMD ["start.sh"]` من الأصل.

---

### ✅ ب-١ — تطبيق Frappe حقيقي — **مُنجز ومُتحقَّق منه** 🔴 الأهم بنيويًا

كان الكود يعيش كـ 15 سكربتًا يُنسخ **داخل إطار العمل نفسه**
(`apps/frappe/frappe/workshop_*.py`) ثم يُنفَّذ واحدًا واحدًا. النتيجة: لا خطافات،
لا ترحيلات، لا إصدارات، لا إلغاء تثبيت، والكود يُمحى مع أي إعادة بناء للصورة،
مع احتمال تصادم مع ملفات frappe عند الترقية. أوضح عَرَض لذلك كان سطر
`from frappe.workshop_futuristic import ...` في سبعة ملفات.

**الآن:** تطبيق `khat_workshop` يُثبَّت بـ `bench install-app` كأي تطبيق آخر.
(أمكن هذا فقط لأن كل الخدمات صارت تبني من صورتنا — ب-٣.)

**قرار تصميمي:** التهيئة تعمل عبر خطاف **`after_migrate`** لا عبر `patches.txt`.
الخطوات idempotent ويجب أن تُعاد في كل نشر لتصل تغييرات الواجهة مع النشر؛
أما الـ patches فتُسجَّل وتعمل **مرة واحدة**، ما يعني إدخال patch جديد مع كل
تعديل بسيط على لوحة.

**مصائد عولجت:**
- `sites/apps.txt` **داخل الـ volume** → يُكتب وقت التشغيل لا وقت البناء، وإلا
  حجبه التركيب.
- فشل `migrate` **ينبّه ولا يوقف**: توقّف `init` يمنع `backend` من الإقلاع أصلًا
  (`service_completed_successfully`) فيحوّل خللًا جزئيًا إلى انقطاع كامل.
- استدعاء من جانب العميل كان ما يزال يشير إلى
  `frappe.workshop_invoice_whatsapp...` — كان سينكسر بصمت بعد النقل.

**التحقق النهائي:**
- `list-apps` → `frappe`، `erpnext`، **`khat_workshop 0.1.0`**
- **صفر ملفات لنا داخل إطار العمل** (`apps/frappe/frappe/workshop_*.py` = 0)
- الخطوات الأربع عشرة كلها `OK` عبر `after_migrate`
- 7/7 لوحات · 20 DocType · صفر أخطاء · `HTTP 200`

---

### 🔵 البنود المتبقية

### ب-١ — تحويل السكربتات إلى تطبيق Frappe مخصص 🔴 الأهم بنيويًا
[`init.sh:134-140`](docker/init.sh#L134-L140) ينسخ الكود المخصص **داخل إطار العمل نفسه**:
```bash
dst="apps/frappe/frappe/${module}.py"
```
النتائج: الكود يُمحى مع كل تحديث للصورة، لا `hooks.py`، لا migrations، لا إصدارات،
ولا إمكانية إلغاء تثبيت. **البديل:** تطبيق `khat_workshop` يُثبَّت بـ `bench install-app`.

### ب-٢ — إلغاء المستندات الموازية
| المخصص | الأصلي في ERPNext |
|---|---|
| `Repair Invoice` (+Item) | **Sales Invoice** |
| `Workshop Quotation` (+Item) | **Quotation** |
| `Workshop Payment` | **Payment Entry** |
| `Service Package` (+Item) | **Product Bundle** |
| `Workshop Technician` | **Employee** (hrms) |

الملف نفسه يعترف بذلك في [سطر 10-11](workshop_gl_stock_integration.py#L10-L11):
*"a **shadow bookkeeping system**, disconnected from the General Ledger"*.
**يحل أيضًا P3ب.**

### ب-٣ — توحيد الصورة على كل الخدمات
`init` وحدها تُبنى من `Dockerfile`؛ أما `backend` و `frontend` و `websocket` والعمال
فتستخدم صورة `frappe/erpnext` الجاهزة.
**الأثر:** أي تطبيق يُضاف إلى Dockerfile **لن يراه backend** — وهو مشغّل النظام الفعلي.
شرط تقني مسبق لأي إضافة تطبيق.

### ب-٤ — الموارد البشرية (`hrms`)
غير مثبّتة (مؤكَّد بـ `list-apps`). لا موظفين ولا رواتب ولا حضور ولا إجازات.
لا يكفي `bench install-app` لأن `apps/` ليس volume → **يجب خبزها في الصورة** (يعتمد على ب-٣).
**يحل أيضًا P6 جذريًا.**

### ب-٥ — إصلاح عيوب الجسر المحاسبي المتبقية
- جسر باتجاه واحد ولمرة واحدة — التعديلات اللاحقة لا تُزامَن ([سطر 16-19](workshop_gl_stock_integration.py#L16-L19))
- **لا معالجة للإلغاء** — إلغاء فاتورة الإصلاح يترك فاتورة المبيعات مُرحَّلة في الدفاتر
- `submit()` داخل `Before Save` → عند فشل الحفظ يبقى مستند مُرحَّل يتيم ومخزون مصروف بلا سند
- اكتشاف المستودع بالاسم العربي `LIKE '%مخازن%'` — هش
- **توثيق الـ Server Scripts المُنشأة يدويًا** (انظر القسم ٤)

### ب-٦ — توحيد طبقة التصميم
كتلة CSS/JS نفسها منسوخة في **٨ ملفات**؛ و`.sidebar-header{display:none}` مكرر ويخفي
تنقلًا معياريًا؛ والتصميم محصور في **shadow DOM** فلا يمتد لبقية الصفحات
([`workshop_dashboard.py:310`](workshop_dashboard.py#L310)).

### ب-٧ — تعدد المستأجرين
حاليًا أحادي المستأجر: [`docker-compose.yml:116`](docker-compose.yml#L116) يثبّت
`FRAPPE_SITE_NAME_HEADER` على موقع واحد.

---

## ٦. سجل القرارات

| التاريخ | القرار | السبب |
|---|---|---|
| 2026-07-21 | فصل العمل إلى مرحلتَي «ديمو» و«إعادة بناء» | إقناع العميل أولًا |
| 2026-07-21 | حذف كتلة `networks` اليدوية | سبب مؤكَّد لـ ERR_CONNECTION_RESET |
| 2026-07-21 | **تأجيل P3ب** (أصناف الفاتورة) للمرحلة ب | `Repair Invoice Item` لا يملك حقل ربط بصنف أصلًا — الحل الوسط سيُرمى |
| 2026-07-21 | إخفاء حقل الموظف بـ Property Setter لا بتعديل الحقول | `make_dt()` يتجاهل الحقول عند وجود الـ DocType |
| 2026-07-21 | عدم حذف `Yemen Tax - KAJ` تلقائيًا | حذف بيانات في الإنتاج يتطلب موافقة صريحة |
| 2026-07-21 | تصفية روابط التقارير بدل إنشاء 4 تقارير | فقدان اختصارات أهون من فقدان اللوحة كلها؛ إنشاء التقارير عمل مستقل |
| 2026-07-21 | نسخ `workshop_futuristic` دون تنفيذه بدل حذفه من القائمة | حذفه يمنع نسخه فينكسر الاستيراد في كل اللوحات |
| 2026-07-21 | **التراجع عن إصلاح P6** | الفرضية كانت خاطئة: `Employee` من ERPNext نواةً لا من `hrms` |

---

## ٧. تحذيرات دائمة

1. **Coolify ينشر من git** — أي تعديل محلي بلا `push` لا أثر له على الخادم.
2. **حاوية `init` تعيد تشغيل كل السكربتات في كل نشر** — لذا تُطبَّق إصلاحات الكود
   تلقائيًا عند النشر، وكلها idempotent.
3. **`make_dt()` لا يعدّل DocType موجودًا** — أي تغيير في الحقول يحتاج
   Property Setter أو Custom Field أو ترحيل صريح.
4. **يوجد منطق حيّ خارج المستودع** (Server Scripts يدوية) — انظر القسم ٤.
5. **لا تُنفَّذ بنود المرحلة ب أثناء المرحلة أ.**
