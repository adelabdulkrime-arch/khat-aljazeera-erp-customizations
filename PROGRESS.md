# خطة المعالجة ومتابعة التقدم — نظام خط الجزيرة ERP

> **الغرض من هذا الملف:** مصدر واحد للحقيقة. كل ما تم تشخيصه موثّق هنا بمراجع دقيقة
> (ملف + رقم سطر) حتى لا نعيد اكتشافه، وكل عمل مُصنّف إلى **مرحلة الديمو** أو
> **إعادة البناء** حتى لا نتشتت بينهما.
>
> **آخر تحديث:** 2026-07-21

---

## ١. السياق والقرار الاستراتيجي

النظام **عُرض على العميل سابقًا عبر ديمو**. القرار الحالي:

| المرحلة | الهدف | المبدأ الحاكم |
|---|---|---|
| **المرحلة أ — الديمو** (الآن) | معالجة ما يراه العميل ويؤثر على قراره | أقل تدخل، أسرع أثر ظاهر. **لا إعادة هيكلة** |
| **المرحلة ب — إعادة البناء** (لاحقًا) | بناء النظام بالشكل الصحيح معماريًا | يُنفَّذ بعد موافقة العميل |

> ⚠️ **قاعدة منع التشتت:** أي بند في المرحلة ب **ممنوع تنفيذه الآن** مهما بدا مغريًا.
> إن ظهرت الحاجة، يُسجَّل في المرحلة ب ويُكمَل العمل الحالي.

---

## ٢. مفتاح الحالات

| الرمز | المعنى |
|---|---|
| ⬜ | لم يبدأ |
| 🟡 | قيد التنفيذ |
| ✅ | مكتمل ومُتحقَّق منه |
| 🔵 | مؤجل للمرحلة ب (قرار مقصود) |
| ⛔ | محجوب بانتظار بند آخر |

---

## ٣. المرحلة أ — أولويات الديمو

### P0 — حاجب الإقلاع: النظام لا يفتح إطلاقًا

- **الحالة:** 🟡 الإصلاح **مدفوع ومنشور** (commit `f9626af`) — بانتظار **التحقق النهائي** فقط
- **العرض:** `ERR_CONNECTION_RESET` عند فتح الرابط
- **السبب الجذري (مؤكَّد من الخادم):** حاوية `frontend` كانت متصلة بـ **٣ شبكات**:

  | الشبكة | IP | Traefik متصل بها؟ |
  |---|---|---|
  | `coolify` | 172.18.0.3 | ✅ |
  | `htgwel6r9845eze7wietga2e` | 172.19.0.11 | ✅ |
  | `htgwel6r9845eze7wietga2e_default` | 172.20.0.2 | ❌ **لا** |

  ولا يوجد وسم `traefik.docker.network` للترجيح → Traefik يختار IP عشوائيًا،
  وعند اختياره `172.20.0.2` لا يملك مسارًا إليه → إعادة تعيين الاتصال.
  **هذا يفسّر «نجح فترة ثم توقف»: النتيجة قرعة عند كل إعادة نشر.**

- **الإصلاح المُطبَّق:** حذف كتلة `networks` اليدوية من `docker-compose.yml`
  (إلغاء الأثر الرجعي للـ commit `96d33f3`) وترك Coolify يدير الشبكات.
- **مرجع التوثيق:** Coolify docs تنصّ صراحة على إزالة تعريفات الشبكات المخصصة.

**الخطوات المتبقية:**
- [x] commit + push إلى `master` — تم في `f9626af` بتاريخ 2026-07-21 05:02
- [ ] التحقق بعد النشر:
  ```bash
  FE=$(docker ps --format '{{.Names}}' | grep -i frontend | head -1)
  docker inspect "$FE" --format '{{range $n,$v := .NetworkSettings.Networks}}{{$n}} -> {{$v.IPAddress}}{{"\n"}}{{end}}'
  ```
  **المتوقع:** شبكة **واحدة فقط** = `htgwel6r9845eze7wietga2e`
- [ ] فتح الرابط والتأكد من ظهور الواجهة

---

### P1 — إنشاء الشركة وإكمال معالج الإعداد ⛔ يحجب P2 و P5

- **الحالة:** ⬜
- **المشكلة:** لا يوجد أي سجل `Company`، ومعالج الإعداد لم يكتمل.
  لا شيء في السكربتات ينشئ شركة (تم التحقق بالبحث الشامل).
- **الأثر المتسلسل — هذا البند يعطّل ثلاثة أشياء أخرى:**
  1. أول دخول يعرض **معالج الإعداد** بدل النظام
  2. حلقات `for cname in frappe.get_all("Company")` في
     [`workshop_oman_setup.py:29`](workshop_oman_setup.py#L29) و `workshop_oman_setup2.py:10`
     تدور على **لا شيء** → ضريبة عُمان وحساب الضريبة **لم تُنشأ أبدًا**
  3. الجسر المحاسبي يقرأ `Global Defaults.default_company` → قيمة فارغة → **الجسر معطّل كليًا**

- [ ] إكمال معالج الإعداد في المتصفح (اسم الشركة، الاختصار، السنة المالية، العملة OMR)
- [ ] التأكد من ضبط `default_company` في Global Defaults

---

### P2 — إعادة تشغيل تهيئة عُمان (ضريبة 5%) ⛔ محجوب بـ P1

- **الحالة:** ⬜
- **ملاحظة مهمة:** هذا **يُصلح نفسه تلقائيًا**. حاوية `init` تعيد تشغيل كل
  السكربتات في كل نشر ([`init.sh:133-146`](docker/init.sh#L133-L146))، وكلها idempotent.
- [ ] بعد إنشاء الشركة → إعادة نشر من Coolify
- [ ] التحقق: وجود حساب `ضريبة القيمة المضافة` وقالب `ضريبة القيمة المضافة عمان 5%`

---

### P3 — الفاتورة المطبوعة: بندان يراهما العميل فورًا

- **الحالة:** ⬜ | **الخطورة:** 🔴 عالية (أحدهما مخالفة ضريبية)

**أ) الفواتير تُرحَّل بدون ضريبة إطلاقًا**
في [`workshop_gl_stock_integration.py:61-80`](workshop_gl_stock_integration.py#L61-L80)
يُنشأ `Sales Invoice` **بدون حقل `taxes`** → قالب ضريبة 5% لا يُطبَّق أبدًا.

**ب) كل البنود تنهار إلى صنف واحد**
[السطر 69](workshop_gl_stock_integration.py#L69): كل سطر يتحول إلى `"item_code": "WORKSHOP-SERVICE"`
→ قطع الغيار لا تظهر بأصنافها الحقيقية في الفاتورة.

- [ ] إضافة قالب الضريبة إلى المستند المُنشأ
- [ ] استخدام الصنف الحقيقي عند توفره بدل `WORKSHOP-SERVICE`

---

### P4 — الشعار مفقود

- **الحالة:** ⬜ | **الخطورة:** 🟠 متوسطة (انطباع بصري مباشر)
- **السبب:** [`workshop_home.py:431`](workshop_home.py#L431) يثبّت مسارًا في الكود:
  `/files/kaj-logo1fe402.png`. اللاحقة `1fe402` هي بصمة يولّدها Frappe عند الرفع اليدوي
  → الملف كان موجودًا على جهاز التطوير فقط. **غير موجود في المستودع ولا يرفعه أي سكربت.**
  ومسار `/files/` يقع داخل volume البيانات (ليس كودًا).
- [ ] رفع الشعار داخل النظام وضبطه في Website Settings (`app_logo`)
- [ ] تعديل الكود ليقرأ من `app_logo` بدل المسار المثبّت

---

### P5 — صفحتا «المستخدمون» و«الأدوار» فارغتان

- **الحالة:** ⬜ | **الخطورة:** 🟠 متوسطة (العميل يضغط فيرى صفحة مكسورة)
- **السبب:** البلاطتان في [`workshop_general_settings.py:17-18`](workshop_general_settings.py#L17-L18)
  تشيران إلى `roles-dashboard` و `users-dashboard` — و**لا شيء ينشئهما إطلاقًا**.
  الدليل: تعليق في [`workshop_translations.py:9`](workshop_translations.py#L9) يذكرهما
  كصفحتين «مفترضتين» — أي صُمِّمتا وتُرجمتا ولم تُكتبا قط.
- **ملاحظة:** صفحات Frappe الأصلية `/app/user` و `/app/role` تعمل بكفاءة تامة.
- [ ] **حل الديمو السريع:** توجيه البلاطتين إلى `/app/user` و `/app/role`
- [ ] 🔵 بناء لوحتين مخصصتين → مؤجل للمرحلة ب

---

### P6 — حقل «الموظف» مكسور في بطاقة الفني

- **الحالة:** ⬜ | **الخطورة:** 🟡 منخفضة
- **السبب:** [`workshop_setup.py:112`](workshop_setup.py#L112) حقل `Link` إلى `Employee`،
  وهذا DocType يعيش في تطبيق **`hrms` غير المثبَّت**.
- [ ] **حل الديمو:** إخفاء الحقل مؤقتًا (تثبيت hrms يتطلب تعديل بنية الصور — مرحلة ب)

---

## ٤. المرحلة ب — إعادة البناء الصحيح (🔵 لا يُنفَّذ الآن)

موثّق هنا حتى لا يضيع، وممنوع العمل عليه قبل موافقة العميل.

### ب-١ — تحويل السكربتات إلى تطبيق Frappe مخصص 🔴 الأهم بنيويًا
[`init.sh:134-140`](docker/init.sh#L134-L140) ينسخ الكود المخصص **داخل إطار العمل نفسه**:
```bash
dst="apps/frappe/frappe/${module}.py"
```
النتائج: الكود يُمحى مع كل تحديث للصورة، لا `hooks.py`، لا migrations، لا إصدارات،
ولا إمكانية إلغاء تثبيت. **البديل:** تطبيق `khat_workshop` يُثبَّت بـ `bench install-app`.

### ب-٢ — إلغاء المستندات الموازية
| المخصص | الأصلي في ERPNext |
|---|---|
| `Repair Invoice` (+Item) | **Sales Invoice** |
| `Workshop Quotation` (+Item) | **Quotation** |
| `Workshop Payment` | **Payment Entry** |
| `Service Package` (+Item) | **Product Bundle** |
| `Workshop Technician` | **Employee** (hrms) |

الملف نفسه يعترف بذلك في [سطر 10-11](workshop_gl_stock_integration.py#L10-L11):
*"a **shadow bookkeeping system**, disconnected from the General Ledger"*.

### ب-٣ — توحيد الصورة على كل الخدمات
حاليًا `init` وحدها تُبنى من `Dockerfile`؛ أما `backend` و `frontend` و `websocket`
والعمال فتستخدم صورة `frappe/erpnext` الجاهزة.
**الأثر:** أي تطبيق يُضاف إلى Dockerfile **لن يراه backend** — وهو مشغّل النظام الفعلي.
هذا شرط تقني مسبق لأي إضافة تطبيق (مثل hrms).

### ب-٤ — الموارد البشرية (`hrms`)
غير مثبّتة إطلاقًا — انفصلت عن ERPNext منذ v14. لا موظفين ولا رواتب ولا حضور ولا إجازات.
لا يكفي `bench install-app` لأن `apps/` ليس volume → **يجب خبزها في الصورة** (يعتمد على ب-٣).

### ب-٥ — إصلاح عيوب الجسر المحاسبي
- جسر باتجاه واحد ولمرة واحدة — التعديلات اللاحقة لا تُزامَن ([سطر 16-19](workshop_gl_stock_integration.py#L16-L19))
- **لا معالجة للإلغاء** — إلغاء فاتورة الإصلاح يترك فاتورة المبيعات مُرحَّلة في الدفاتر
- `submit()` داخل `Before Save` ([سطر 43](workshop_gl_stock_integration.py#L43)، [82](workshop_gl_stock_integration.py#L82)) → عند فشل الحفظ يبقى مستند مُرحَّل يتيم ومخزون مصروف بلا سند
- اكتشاف المستودع بالاسم العربي `LIKE '%مخازن%'` ([سطر 25](workshop_gl_stock_integration.py#L25)) — هش

### ب-٦ — توحيد طبقة التصميم
- كتلة CSS/JS نفسها منسوخة في **٨ ملفات**
- `.sidebar-header { display:none !important }` مكرر ويخفي تنقلًا معياريًا
- التصميم محصور في **shadow DOM** داخل Custom HTML Block → لا يمتد لبقية الصفحات
  (موثّق في [`workshop_dashboard.py:310`](workshop_dashboard.py#L310))

### ب-٧ — تعدد المستأجرين (إن أردنا نموذج المؤجّر)
حاليًا أحادي المستأجر: [`docker-compose.yml:116`](docker-compose.yml#L116) يثبّت
`FRAPPE_SITE_NAME_HEADER` على موقع واحد. التحويل يتطلب توجيهًا حسب النطاق + موقع لكل عميل.

---

## ٥. سجل القرارات

| التاريخ | القرار | السبب |
|---|---|---|
| 2026-07-21 | فصل العمل إلى مرحلتَي «ديمو» و«إعادة بناء» | إقناع العميل أولًا، ثم البناء الصحيح |
| 2026-07-21 | حذف كتلة `networks` اليدوية من compose | مخالفة لتوصيات Coolify وسبب مؤكَّد لـ ERR_CONNECTION_RESET |
| 2026-07-21 | تأجيل `hrms` للمرحلة ب | يعتمد على توحيد الصور (ب-٣) — لا يمكن حلّه بسرعة |

---

## ٦. تحذيرات دائمة

1. **Coolify ينشر من git** — أي تعديل محلي بلا `push` **لا أثر له** على الخادم.
2. **حاوية `init` تعيد تشغيل كل السكربتات في كل نشر** — وهذا مفيد: بعض المشاكل
   (مثل P2) تُصلح نفسها بإعادة نشر بعد استيفاء شرطها.
3. **لا تُنفَّذ بنود المرحلة ب أثناء المرحلة أ.**
4. **ترتيب الحجب:** P0 → P1 → (P2, P3) — الباقي مستقل.
