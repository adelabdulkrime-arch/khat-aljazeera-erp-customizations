# خطة المعالجة ومتابعة التقدم — نظام خط الجزيرة ERP

> **الغرض من هذا الملف:** مصدر واحد للحقيقة. كل ما تم تشخيصه موثّق هنا بمراجع دقيقة
> (ملف + رقم سطر) حتى لا نعيد اكتشافه، وكل عمل مُصنّف إلى **مرحلة الديمو** أو
> **إعادة البناء** حتى لا نتشتت بينهما.
>
> **آخر تحديث:** 2026-07-21 — بعد التحقق المباشر من الخادم عبر SSH

---

## ١. السياق والقرار الاستراتيجي

النظام **عُرض على العميل سابقًا عبر ديمو**. القرار الحالي:

| المرحلة | الهدف | المبدأ الحاكم |
|---|---|---|
| **المرحلة أ — الديمو** (الآن) | معالجة ما يراه العميل ويؤثر على قراره | أقل تدخل، أسرع أثر ظاهر. **لا إعادة هيكلة** |
| **المرحلة ب — إعادة البناء** (لاحقًا) | بناء النظام بالشكل الصحيح معماريًا | يُنفَّذ بعد موافقة العميل |

> ⚠️ **قاعدة منع التشتت:** أي بند في المرحلة ب **ممنوع تنفيذه الآن** مهما بدا مغريًا.

### 🔑 معطى حاسم: النظام خالٍ من البيانات

تحقُّق مباشر من قاعدة البيانات: **صفر فاتورة إصلاح، وصفر فاتورة مبيعات.**
النظام حاليًا **إعدادات فقط بلا أي معاملات**. أثر ذلك:

- عيوب الجسر المحاسبي **لم تُنتج بيانات خاطئة بعد** — أُصلحت قبل أول إدخال.
- **لا توجد مخاطر ترحيل بيانات إطلاقًا** → هذه هي اللحظة المثالية والأرخص لتنفيذ
  المرحلة ب. كل يوم إدخال بيانات فعلي يرفع تكلفتها.

---

## ٢. مفتاح الحالات

| الرمز | المعنى |
|---|---|
| ⬜ | لم يبدأ |
| 🟡 | الكود جاهز — بانتظار نشر/تحقق |
| ✅ | مكتمل ومُتحقَّق منه فعليًا على الخادم |
| 🔵 | مؤجل للمرحلة ب (قرار مقصود) |
| 👤 | يتطلب إجراءً بشريًا (لا يمكن أتمتته) |

---

## ٣. المرحلة أ — أولويات الديمو

### ✅ P0 — حاجب الإقلاع: `ERR_CONNECTION_RESET` — **مُغلق ومُتحقَّق منه**

- **السبب الجذري:** حاوية `frontend` كانت على **٣ شبكات**، إحداها (`_default` / 172.20)
  **لا يملك Traefik أي مسار إليها**، ولا يوجد وسم `traefik.docker.network` للترجيح
  → اختيار عشوائي للـ IP عند كل إعادة نشر. **هذا يفسّر «نجح فترة ثم توقف».**
- **الإصلاح:** حذف كتلة `networks` اليدوية من `docker-compose.yml` (commit `f9626af`).
- **التحقق النهائي (2026-07-21):**
  - الواجهة الآن على شبكة **واحدة فقط**: `htgwel6r9845eze7wietga2e → 172.19.0.11`
  - `coolify-proxy` يشترك معها في تلك الشبكة بالضبط
  - **`HTTP 200` خلال 0.4 ثانية** ✅
  - كل الحاويات تعمل، و`init` و`configurator` انتهيا بـ `Exited (0)`

---

### ✅ P1 — الشركة ومعالج الإعداد — **كان منجزًا مسبقًا**

> ⚠️ **تصحيح:** التشخيص الأولي افترض عدم وجود شركة. الفحص المباشر أثبت العكس.

| المفتاح | القيمة الفعلية |
|---|---|
| `Company` | **Khat Al Jazeera** |
| `setup_complete` | `1` |
| `default_company` | Khat Al Jazeera |
| `default_currency` | OMR |

---

### ✅ P2 — تهيئة عُمان وضريبة 5% — **كان منجزًا مسبقًا**

قالب `ضريبة القيمة المضافة عمان 5% - KAJ` موجود و`is_default = 1`،
وحسابه `ضريبة القيمة المضافة - KAJ` بنسبة 5%.
كما تأكد إنشاء **20** DocType مخصصًا و**7** حالات بطاقة عمل.

---

### ✅ P7 — خمس لوحات مفقودة — **أخطر ما اكتُشف، وأُصلح**

> لم يكن هذا البند في الخطة الأصلية — ظهر أثناء التحقق من الخادم.

**العرض:** الشريط الجانبي كان يعرض `Home` و`General Settings` فقط. أما لوحات
**المحاسبة والمخزون والمشتريات والمبيعات والورشة** فكانت **غير موجودة إطلاقًا** —
أي أن العميل كان سيرى نظامًا شبه فارغ.

**الأسباب الجذرية (من سجل `init` على الخادم):**

| الوحدة | سبب الفشل الفعلي |
|---|---|
| `workshop_accounting` | `LinkValidationError: Could not find Row #3: Role: محاسب` |
| `workshop_inventory` | الدور `مدير المستودع` غير موجود |
| `workshop_purchasing` | الدور `مسؤول المشتريات` غير موجود |
| `workshop_sales` | الدوران `مندوب مبيعات` و`كاشير` غير موجودان |
| `workshop_dashboard` | 4 تقارير غير موجودة (الفنيين، العمولات، الإيرادات، بطاقات العمل) |
| `workshop_futuristic` | مكتبة مشتركة بلا `execute()` — كانت مُدرجة خطأً في قائمة التشغيل |

الكتل تُسند أدوارًا عربية في جدول `roles`، و`workshop_setup` كان ينشئ
`Workshop Manager` فقط → فشل الإدراج → **اختفت اللوحات صامتةً في كل نشر**.

**الإصلاحات:**
1. إنشاء كل الأدوار المطلوبة مسبقًا في [`workshop_setup.py`](workshop_setup.py)
2. تصفية روابط التقارير إلى الموجود فعليًا في [`workshop_dashboard.py`](workshop_dashboard.py)
   — فقدان 4 اختصارات أهون من فقدان اللوحة بأكملها
3. نقل `workshop_futuristic` إلى قائمة `LIBS` في [`init.sh`](docker/init.sh):
   **تُنسخ ولا تُنفَّذ** (النسخ ضروري وإلا انكسر الاستيراد في اللوحات)

**التحقق النهائي:** الوحدات الأربع عشرة كلها `OK` و**صفر تحذيرات لأول مرة**،
واللوحات السبع موجودة، والأدوار الخمسة أُنشئت.

---

### ✅ P3أ — الفواتير كانت تُرحَّل بلا ضريبة — **أُصلح ومُتحقَّق منه**

- **الخطورة:** 🔴 مخالفة ضريبية، وليست عيبًا بصريًا.
- **السبب:** `Sales Invoice` المُولَّد تلقائيًا كان يُنشأ **بدون حقل `taxes` إطلاقًا**
  → قالب ضريبة 5% لا يُطبَّق أبدًا.
- **الإصلاح:** [`workshop_gl_stock_integration.py`](workshop_gl_stock_integration.py) —
  جلب القالب الافتراضي للشركة ونسخ صفوفه إلى الفاتورة (`taxes_and_charges` + `taxes`).
- **تحقُّق مسبق:** يوجد قالب افتراضي **واحد فقط** (`is_default=1`) وهو قالب عُمان،
  إذًا الاستعلام حتمي ولا التباس فيه.

---

### 🔵 P3ب — انهيار بنود الفاتورة إلى صنف واحد — **مؤجل للمرحلة ب (بقرار)**

- كل سطر يتحول إلى `"item_code": "WORKSHOP-SERVICE"`.
- **سبب التأجيل — ليس كسلًا بل قيد بنيوي:** فحص `Repair Invoice Item` أثبت أنه
  **لا يحتوي على أي حقل ربط بصنف حقيقي** (حقوله: وصف نصي، كمية، سعر، إجمالي فقط).
  أي أنه **لا يوجد صنف حقيقي أصلًا ليُستخدم**. الإصلاح الصحيح هو إلغاء
  `Repair Invoice` والانتقال إلى `Sales Invoice` مباشرة (بند **ب-٢**) — وأي حل وسط
  الآن سيُرمى لاحقًا.
- **الأثر المقبول مؤقتًا:** الفاتورة المطبوعة **تعرض الوصف العربي الصحيح لكل سطر**
  (لأن `item_name`/`description` تُنسخ من السطر)، فالضرر منحصر في تقارير
  المخزون والربحية على مستوى الصنف — وهي خارج نطاق الديمو.

---

### ✅ P4 — الشعار — **أُصلح ومُتحقَّق منه** + 👤 يتطلب رفع الشعار

- **السبب:** [`workshop_home.py`](workshop_home.py) كان يثبّت `/files/kaj-logo1fe402.png`
  في الكود. اللاحقة `1fe402` بصمة يولّدها Frappe عند الرفع اليدوي → الملف كان على
  جهاز التطوير فقط.
- **تحقُّق قاطع:** مجلد `sites/<site>/public/files/` على الخادم **فارغ تمامًا**.
- **الإصلاح:** إزالة المسار المثبَّت؛ الشعار يُقرأ الآن من Website Settings،
  وإن لم يوجد **يُخفى العنصر** بدل عرض صورة مكسورة.
- 👤 **مطلوب منك:** رفع الشعار وضبطه في `Website Settings → app_logo` فيظهر تلقائيًا.

---

### ✅ P5 — صفحتا «المستخدمون» و«الأدوار» — **أُصلح ومُتحقَّق منه**

- **السبب:** البلاطتان كانتا تشيران إلى `roles-dashboard` و `users-dashboard`،
  و**لا شيء ينشئهما** (تأكد من الخادم: كلتاهما `MISSING`).
- **الإصلاح:** توجيههما إلى قائمتَي `Role` و `User` المدمجتين (تعمل بكفاءة تامة)
  في [`workshop_general_settings.py`](workshop_general_settings.py)
  و[`workshop_futuristic.py`](workshop_futuristic.py) (لوحة الأوامر).
- 🔵 بناء لوحتين مخصصتين → المرحلة ب.

---

### ❌ P6 — حقل «الموظف» — **تشخيص خاطئ، لا توجد مشكلة أصلًا**

> ⚠️ **تصحيح صريح لخطأ ارتكبته:** ادّعيتُ أن `Employee` يعيش في تطبيق `hrms`
> وأن الحقل مكسور. **هذا غير صحيح.**

**ما أثبته الخادم:** `Employee` موجود ويأتي مع **ERPNext نواةً**، ضمن الوحدة
`Setup` — وليس ضمن `hrms`. إذًا الحقل **يعمل بشكل سليم تمامًا** ولم يكن معطلًا قط.

**الإجراء:** حُذف الـ Property Setter الذي أضفته في الدفعة السابقة. كان كودًا ميتًا
مبنيًا على فرضية باطلة، وتعليقه كان مضللًا لمن يقرأ الكود لاحقًا.

**ما يبقى صحيحًا:** تطبيق `hrms` فعلًا غير مثبَّت، فوحدات **الرواتب والحضور
والإجازات وتقييم الأداء** غائبة — لكن هذا لا علاقة له بحقل الموظف (بند **ب-٤**).

---

## ٤. ملاحظات مكتشفة أثناء الفحص (غير حاجبة)

| الملاحظة | التفصيل | الإجراء |
|---|---|---|
| قالب ضريبي غريب | `Yemen Tax - KAJ` (حساب `VAT - KAJ`، 5%) — بقايا إعداد سابق | غير ضار حاليًا لأن `is_default = 0`؛ **يُقترح حذفه** بعد موافقتك |
| Server Scripts غير موثّقة | `Update Invoice On Payment` و `Update Invoice On Payment Delete` **ليسا في المستودع** — أُنشئا يدويًا عبر الواجهة | خطر: سيُفقدان عند أي إعادة بناء. يجب توثيقهما في الكود |

> ⚠️ الملاحظة الثانية مهمة: وجود منطق حيّ خارج المستودع يعني أن **المستودع ليس
> المصدر الكامل للحقيقة** حاليًا. يجب معالجته ضمن المرحلة ب.

---

## ٥. المرحلة ب — إعادة البناء الصحيح

> **الحالة:** بدأ التنفيذ في 2026-07-22 بعد اقتناع العميل وتفويض كامل.

### ✅ ب-٠ — النسخ الاحتياطية الآلية — **مُنجز ومُتحقَّق منه** 🔴 لم يكن في الخطة

اكتُشف أثناء فحص ما قبل التنفيذ: **النظام كان بلا أي نسخة احتياطية إطلاقًا.**

- مجلد النسخ كان فارغًا · لا مهام `cron` على المضيف
- المهمة المجدولة الوحيدة `delete_downloadable_backups` — **تحذف ولا تنشئ**
- **السبب:** في Frappe العادي تكون النسخ التلقائية مهمة `cron` على **المضيف**،
  وهذا المضيف غير موجود داخل الحاويات → نشر Docker بلا نسخ صامتة

**الحل:** خدمة `backup` مخصصة في `docker-compose.yml` تعمل بفاصل زمني قابل
للضبط (`BACKUP_INTERVAL_SECONDS`) مع مدة احتفاظ (`BACKUP_RETENTION_DAYS`).

**قرار مهم:** النسخ تُكتب في **volume منفصل** (`backups`) لا في `sites`، حتى لا
يؤدي تلف بيانات الموقع إلى ضياع النسخ معها.

**مصيدة واجهتها:** أول نسخة فشلت برسالة Frappe المضلِّلة «قاعدة البيانات قد تكون
تالفة». السبب الحقيقي **صلاحيات**: Docker ينشئ نقطة الـ volume مملوكة لـ `root`
بينما الحاوية تعمل كـ `frappe`. عولج بإنشاء `/backups` في `Dockerfile` مملوكًا
لـ `frappe` + تصحيح ملكية الـ volume القائم.

**التحقق:** 12 ملفًا · 8.9MB · آخر دورة `[backup] OK`.

**✅ اختبار الاستعادة (2026-07-22):** نسخة لم تُختبَر استعادتها ليست نسخة. أحدث
نسخة استُعيدت فعليًا إلى قاعدة بيانات مؤقتة:

| الفحص | النتيجة |
|---|---|
| سلامة gzip | سليم |
| علامة `Dump completed` | موجودة (لم يُبتَر) |
| حجم الاستعادة | 34MB |
| الجداول المستعادة | **913** |
| DocTypes · مساحات العمل · المستخدمون | **981** · **34** · **3** |

ثم حُذفت القاعدة المؤقتة. **النسخ مُثبَتة الاستعادة، لا مجرد موجودة.**

⚠️ **الخطر الوحيد المتبقي:** النسخ ما زالت على **نفس الخادم** — تلف القرص يفقد
البيانات والنسخ معًا. Frappe v15+ لم يعد يشحن تكاملات النسخ الخارجي في النواة
(تحقّقتُ: لا `S3 Backup Settings` ولا `Dropbox` ولا `Google Drive`)، فالحل يحتاج
إضافة صريحة **ومفاتيح تخزين منك**. لن أشحن كودًا لا أستطيع اختباره.

---

### ✅ ب-٣ — توحيد الصورة على كل الخدمات — **مُنجز ومُتحقَّق منه**

كانت `init` وحدها تُبنى من `Dockerfile`، وبقية الخدمات تستخدم `frappe/erpnext`
الجاهزة → أي شيء يُخبز في الصورة **يراه `init` فقط** ولا يراه `backend` الذي
يُشغّل النظام فعلًا. كان هذا **الحاجب التقني** أمام أي تطبيق مخصص أو `hrms`.

**الحل:** مرساة YAML واحدة (`x-frappe-image`) تُطبَّق على **9 خدمات Frappe**.
قاعدة البيانات و Redis لم تُمَس. الطبقات مشتركة فالكلفة على الذاكرة المؤقتة لا القرص.

**التحقق:** 7 خدمات تعمل على صورتنا · `/backups` موجود داخل `backend` (دليل قاطع).

> 🔴 **درس من انحدار تسبّبتُ به:** بعد التوحيد ظهر **502**. السبب أن `Dockerfile`
> كان ينتهي بـ `CMD ["/opt/init.sh"]`، فورث `backend` هذا الأمر وشغّل سكربت
> التهيئة **بدل gunicorn**، فبقي nginx بلا upstream. كان الأمر غير ضار ما دامت
> `init` وحدها تستخدم الصورة. **العبرة:** توحيد الصور يكشف كل افتراض ضمني في
> `CMD`/`ENTRYPOINT`. عولج بحذف التجاوز ووراثة `CMD ["start.sh"]` من الأصل.

---

### ✅ ب-١ — تطبيق Frappe حقيقي — **مُنجز ومُتحقَّق منه** 🔴 الأهم بنيويًا

كان الكود يعيش كـ 15 سكربتًا يُنسخ **داخل إطار العمل نفسه**
(`apps/frappe/frappe/workshop_*.py`) ثم يُنفَّذ واحدًا واحدًا. النتيجة: لا خطافات،
لا ترحيلات، لا إصدارات، لا إلغاء تثبيت، والكود يُمحى مع أي إعادة بناء للصورة،
مع احتمال تصادم مع ملفات frappe عند الترقية. أوضح عَرَض لذلك كان سطر
`from frappe.workshop_futuristic import ...` في سبعة ملفات.

**الآن:** تطبيق `khat_workshop` يُثبَّت بـ `bench install-app` كأي تطبيق آخر.
(أمكن هذا فقط لأن كل الخدمات صارت تبني من صورتنا — ب-٣.)

**قرار تصميمي:** التهيئة تعمل عبر خطاف **`after_migrate`** لا عبر `patches.txt`.
الخطوات idempotent ويجب أن تُعاد في كل نشر لتصل تغييرات الواجهة مع النشر؛
أما الـ patches فتُسجَّل وتعمل **مرة واحدة**، ما يعني إدخال patch جديد مع كل
تعديل بسيط على لوحة.

**مصائد عولجت:**
- `sites/apps.txt` **داخل الـ volume** → يُكتب وقت التشغيل لا وقت البناء، وإلا
  حجبه التركيب.
- فشل `migrate` **ينبّه ولا يوقف**: توقّف `init` يمنع `backend` من الإقلاع أصلًا
  (`service_completed_successfully`) فيحوّل خللًا جزئيًا إلى انقطاع كامل.
- استدعاء من جانب العميل كان ما يزال يشير إلى
  `frappe.workshop_invoice_whatsapp...` — كان سينكسر بصمت بعد النقل.

**التحقق النهائي:**
- `list-apps` → `frappe`، `erpnext`، **`khat_workshop 0.1.0`**
- **صفر ملفات لنا داخل إطار العمل** (`apps/frappe/frappe/workshop_*.py` = 0)
- الخطوات الأربع عشرة كلها `OK` عبر `after_migrate`
- 7/7 لوحات · 20 DocType · صفر أخطاء · `HTTP 200`

---

### ✅ ب-٦ — توحيد طبقة التصميم (الجزء المكرَّر) — **مُنجز ومُثبَت**

`NAV_LANG_JS` (مبدّل اللغة + زر الخروج + إخفاء ترويسة الشريط الجانبي) كان
معرَّفًا **متطابقًا بايت-ببايت** في **سبعة** ملفات لوحات. نُقل إلى المكتبة
المشتركة `workshop_futuristic.py` — نفس الوحدة التي كانت تشارك `FUTURISTIC_CSS`
و`FUTURISTIC_JS` مع هذه الملفات السبعة بالذات.

**النتيجة:** 553 حذفًا مقابل 92 إضافة = **461 سطرًا صافيًا أقل**.

**قرار مقصود:** `ICON_SPRITE_JS` **لم يُمَس**. البصمات أظهرت **ثلاث صيغ مختلفة
فعليًا** (الرئيسية، لوحة التحكم، وواحدة مشتركة للخمس الباقية) — أي أنها مجموعات
أيقونات خاصة بكل صفحة، لا تكرارًا.

**الإثبات (لا الافتراض):** التُقطت بصمات `MD5` للسكربتات السبعة المخزَّنة في
قاعدة البيانات **قبل** التغيير، وقورنت **بعد** النشر:

| الكتلة | قبل | بعد |
|---|---|---|
| Accounting_Dashboard_Tiles | `362053ac71` | `362053ac71` ✓ |
| General_Settings_Tiles | `6dd95ab1cd` | `6dd95ab1cd` ✓ |
| Home_Dashboard_Tiles | `f940bef716` | `f940bef716` ✓ |
| Inventory_Dashboard_Tiles | `915d66436a` | `915d66436a` ✓ |
| Purchasing_Dashboard_Tiles | `9742682d1a` | `9742682d1a` ✓ |
| Sales_Dashboard_Tiles | `9788feb68d` | `9788feb68d` ✓ |
| Workshop_Dashboard_Search | `7a6a4c53fa` | `7a6a4c53fa` ✓ |

**تطابق تام** → التوحيد لا يغيّر السلوك ولو بايتًا واحدًا.

**الجزء الثاني — الترقية إلى أصل حقيقي — ✅ مُنجز أيضًا:**

طبقة شريط التنقل صارت ملفًا حقيقيًا `public/js/desk.js` يُحمَّل عبر
`app_include_js`، فيعمل **مرة واحدة على كل صفحة ديسك** بدل سبع مرات على
اللوحات وحدها. هذا يعالج مباشرةً الشكوى السابقة: «الشريط يختلف فور فتح
`/app/user`».

**التحقق:**
- `app_include_js` يتضمّن `/assets/khat_workshop/js/desk.js` بجانب حِزَم frappe/erpnext
- الأصل يُخدَم: `HTTP 200` · 4470 بايت
- **صفر من أصل 7** كتل ما زالت تُضمّن الكود · صفر أخطاء

**مصيدتان تعلّمناهما:**
1. **`node`/`yarn`/`npm` غير موجودة إطلاقًا** في صورة التشغيل (أصولها مبنية
   مسبقًا) → استُخدم ملف `.js` عادي لا `.bundle.js` لأن الأخير يحتاج esbuild.
2. **`sites/assets` ليس مجلدًا في الـ volume.** نقطة دخول الصورة تنفّذ عند
   **كل** إقلاع حاوية:
   ```
   rm -rf sites/assets && ln -s /home/frappe/frappe-bench/assets sites/assets
   ```
   فأي رابط يُنشأ وقت التشغيل يُمحى عند الإقلاع التالي — وهذا ما حدث في
   المحاولة الأولى: `init.sh` سجّل نجاحًا ولم يُبقِ شيئًا. الحل: خبز الرابط في
   `Dockerfile` بنفس نمط الصورة نفسها:
   ```
   assets/frappe        -> apps/frappe/frappe/public
   assets/erpnext       -> apps/erpnext/erpnext/public
   assets/khat_workshop -> apps/khat_workshop/khat_workshop/public
   ```

---

### 🔵 البنود المتبقية

### ب-١ — تحويل السكربتات إلى تطبيق Frappe مخصص 🔴 الأهم بنيويًا
[`init.sh:134-140`](docker/init.sh#L134-L140) ينسخ الكود المخصص **داخل إطار العمل نفسه**:
```bash
dst="apps/frappe/frappe/${module}.py"
```
النتائج: الكود يُمحى مع كل تحديث للصورة، لا `hooks.py`، لا migrations، لا إصدارات،
ولا إمكانية إلغاء تثبيت. **البديل:** تطبيق `khat_workshop` يُثبَّت بـ `bench install-app`.

### ✅ ب-٢ — إلغاء المستندات الموازية — **مُنجز ومُتحقَّق منه**

المستخدمون يعملون الآن مباشرةً في `Quotation` و`Sales Invoice` و`Payment Entry`،
والرابط بالورشة انتقل إلى **حقول مخصصة** على تلك المستندات (`work_card`,
`vehicle`) فلم يُفقد شيء.

**ما حُذف:** `Repair Invoice` (+Item) · `Workshop Quotation` (+Item) ·
`Workshop Payment` · `Service Package` (+Item) — سبعة DocTypes.

**ما بقي (12 خاصًا بالورشة فعلًا، بلا مقابل في ERPNext):** `Work Card` وجداوله ·
`Customer Vehicle` · `Vehicle Brand/Model` · `Workshop Technician` ·
`Work Card Status` · `Commission Log` · `Maintenance Reminder` ·
`Workshop Settings`.

**الجسر اختفى ومعه عيوبه كلها:** لا فواتير بلا ضريبة، ولا انهيار البنود إلى
`WORKSHOP-SERVICE`، ولا غياب معالجة الإلغاء، ولا `submit()` داخل `Before Save`
يخلّف مستندات مُرحَّلة يتيمة. حُذفت أيضًا الـ Server Scripts التي أُنشئت يدويًا
ولم تكن في المستودع أصلًا.

**حارس أمان مقصود:** [`workshop_retire_shadow.py`](khat_workshop/khat_workshop/setup/workshop_retire_shadow.py)
يحذف DocType **فقط** إذا كان **خالياً من المستندات**؛ ولو وُجدت بيانات لأبقاه
وأبلغ. استرجاع البيانات أهم من إتمام الترحيل في موعده.

**التحقق:** `RETIRE_SHADOW dropped=[السبعة] kept_with_data=none` · 4/4 اختفت ·
الحقول المخصصة أُنشئت على المستندين · 15 خطوة `OK` · صفر أخطاء · `HTTP 200`.
نسخة احتياطية أُخذت قبل التنفيذ (56 ملفًا).

---

### ب-٢ (المرجع الأصلي) — إلغاء المستندات الموازية
| المخصص | الأصلي في ERPNext |
|---|---|
| `Repair Invoice` (+Item) | **Sales Invoice** |
| `Workshop Quotation` (+Item) | **Quotation** |
| `Workshop Payment` | **Payment Entry** |
| `Service Package` (+Item) | **Product Bundle** |
| `Workshop Technician` | **Employee** (hrms) |

الملف نفسه يعترف بذلك في [سطر 10-11](workshop_gl_stock_integration.py#L10-L11):
*"a **shadow bookkeeping system**, disconnected from the General Ledger"*.
**يحل أيضًا P3ب.**

### ب-٣ — توحيد الصورة على كل الخدمات
`init` وحدها تُبنى من `Dockerfile`؛ أما `backend` و `frontend` و `websocket` والعمال
فتستخدم صورة `frappe/erpnext` الجاهزة.
**الأثر:** أي تطبيق يُضاف إلى Dockerfile **لن يراه backend** — وهو مشغّل النظام الفعلي.
شرط تقني مسبق لأي إضافة تطبيق.

### ✅ ب-٤ — الموارد البشرية (`hrms`) — **مُنجز ومُتحقَّق منه**

نُفِّذ ببناء **من مرحلتين**: مرحلة أولى تثبّت `node`+`yarn` وتشغّل
`bench get-app --branch version-16 hrms` ثم `bench build --app hrms`؛ ومرحلة
ثانية هي صورة التشغيل تنسخ **الناتج فقط** — فتبقى أدوات البناء وتاريخ git
خارج الإنتاج.

**ثلاث عقبات حُلَّت، كلها مُختبَرة ببناء يدوي على الخادم قبل الدفع:**

1. **`assets.json` يجب أن يُدمَج لا أن يُستبدل.** `bench build --app hrms`
   يعيد كتابته بمحتوى التطبيق المبني وحده — **401 بايت** مقابل **3671** في
   الصورة الأساسية. نسخه كما هو كان سيُسقط كل ربط حِزَم `frappe` و`erpnext`
   ويكسر **النظام كله** لا الموارد البشرية فقط.
   الحل: [`docker/merge-assets.py`](docker/merge-assets.py) يدمج **ويرفض** كتابة
   بيان أصغر مما بدأ به. المرصود: `43 → 48` و`11 → 12`.
2. **الحجم.** الاستنساخ ~806MB، وحذف `node_modules` العلوي وحده أبقى 790MB لأن
   `frontend/` و`roster/` تطبيقا SPA لهما `node_modules` متداخلة. التنظيف
   التعاودي أنزل `apps/hrms` إلى **53MB**، والصورة النهائية **3.88GB** مقابل
   **3.79GB** للأساس — أي **~90MB فقط**.
3. **تحذير `install-app hrms failed`** ظهر في السجل، لكن الفحص أثبت أن التثبيت
   **اكتمل**: الخطأ عابر أثناء نقل `Expense Claim Type` من الوحدة `Core` إلى
   `HR`، و`bench migrate` أتمّه.

**التحقق النهائي:** 4 تطبيقات مثبَّتة · **160 DocType** في `HR`/`Payroll` ·
مساحتا عمل `HR Setup` و`Payroll` · حِزمة `hrms.bundle` تُخدَم بـ `HTTP 200` ·
لوحات الورشة 7/7 سليمة · صفر أخطاء.

> ⚠️ **تصحيح لادعاء سابق مني:** قلتُ إن الصورة «لا تحوي node إطلاقًا». **خطأ** —
> الصورة الأساسية تشحن `node` عبر `nvm`، لكنه خارج `PATH` الافتراضي في بعض
> الأصداف، ولذلك جاء فحصي الأول فارغًا. مرحلة البناء تثبّت `node` صراحةً بدل
> الاعتماد على ذلك.

---

### ⛔ ب-٤ (التقدير القديم — محفوظ للسياق)

> **تحديث 2026-07-22 بعد فحص فعلي:** هذا البند **ليس** إضافة سطر إلى `Dockerfile`.

صورة `frappe/erpnext` هي صورة **تشغيل** بأصول مبنية مسبقًا، و**لا تحوي
`node` ولا `yarn` ولا `npm`** (تحقّقتُ: الثلاثة مفقودة). لذا:

- `bench get-app hrms` سيستنسخ ويثبّت الحزمة، لكنه **لن يبني أصول الواجهة**
- بلا أصول مبنية تصبح شاشات الموارد البشرية **مكسورة** — أسوأ من غيابها

**ما يتطلبه فعلًا:** إعادة هيكلة مسار بناء الصورة إلى نمط
`frappe_docker` متعدد المراحل (`frappe/base` + `apps.json` + مرحلة بناء فيها
`node` تشغّل `bench build`). أي **تغيير كامل لكيفية إنتاج الصورة**، على نظام
يخدم العميل حاليًا.

> ⚠️ **قبل إنفاق هذه المخاطرة:** لم يتأكّد بعد أن الموارد البشرية ضمن نطاق
> المشروع أصلًا. يستحق الحسم قبل التنفيذ.

### ب-٤ (المرجع الأصلي) — الموارد البشرية (`hrms`)
غير مثبّتة (مؤكَّد بـ `list-apps`). لا موظفين ولا رواتب ولا حضور ولا إجازات.
لا يكفي `bench install-app` لأن `apps/` ليس volume → **يجب خبزها في الصورة** (يعتمد على ب-٣).
**يحل أيضًا P6 جذريًا.**

### ب-٥ — إصلاح عيوب الجسر المحاسبي المتبقية
- جسر باتجاه واحد ولمرة واحدة — التعديلات اللاحقة لا تُزامَن ([سطر 16-19](workshop_gl_stock_integration.py#L16-L19))
- **لا معالجة للإلغاء** — إلغاء فاتورة الإصلاح يترك فاتورة المبيعات مُرحَّلة في الدفاتر
- `submit()` داخل `Before Save` → عند فشل الحفظ يبقى مستند مُرحَّل يتيم ومخزون مصروف بلا سند
- اكتشاف المستودع بالاسم العربي `LIKE '%مخازن%'` — هش
- **توثيق الـ Server Scripts المُنشأة يدويًا** (انظر القسم ٤)

### ب-٦ — توحيد طبقة التصميم
كتلة CSS/JS نفسها منسوخة في **٨ ملفات**؛ و`.sidebar-header{display:none}` مكرر ويخفي
تنقلًا معياريًا؛ والتصميم محصور في **shadow DOM** فلا يمتد لبقية الصفحات
([`workshop_dashboard.py:310`](workshop_dashboard.py#L310)).

### ب-٧ — تعدد المستأجرين
حاليًا أحادي المستأجر: [`docker-compose.yml:116`](docker-compose.yml#L116) يثبّت
`FRAPPE_SITE_NAME_HEADER` على موقع واحد.

---

## ٦. سجل القرارات

| التاريخ | القرار | السبب |
|---|---|---|
| 2026-07-21 | فصل العمل إلى مرحلتَي «ديمو» و«إعادة بناء» | إقناع العميل أولًا |
| 2026-07-21 | حذف كتلة `networks` اليدوية | سبب مؤكَّد لـ ERR_CONNECTION_RESET |
| 2026-07-21 | **تأجيل P3ب** (أصناف الفاتورة) للمرحلة ب | `Repair Invoice Item` لا يملك حقل ربط بصنف أصلًا — الحل الوسط سيُرمى |
| 2026-07-21 | إخفاء حقل الموظف بـ Property Setter لا بتعديل الحقول | `make_dt()` يتجاهل الحقول عند وجود الـ DocType |
| 2026-07-21 | عدم حذف `Yemen Tax - KAJ` تلقائيًا | حذف بيانات في الإنتاج يتطلب موافقة صريحة |
| 2026-07-21 | تصفية روابط التقارير بدل إنشاء 4 تقارير | فقدان اختصارات أهون من فقدان اللوحة كلها؛ إنشاء التقارير عمل مستقل |
| 2026-07-21 | نسخ `workshop_futuristic` دون تنفيذه بدل حذفه من القائمة | حذفه يمنع نسخه فينكسر الاستيراد في كل اللوحات |
| 2026-07-21 | **التراجع عن إصلاح P6** | الفرضية كانت خاطئة: `Employee` من ERPNext نواةً لا من `hrms` |

---

## ٧. تحذيرات دائمة

1. **Coolify ينشر من git** — أي تعديل محلي بلا `push` لا أثر له على الخادم.
2. **حاوية `init` تعيد تشغيل كل السكربتات في كل نشر** — لذا تُطبَّق إصلاحات الكود
   تلقائيًا عند النشر، وكلها idempotent.
3. **`make_dt()` لا يعدّل DocType موجودًا** — أي تغيير في الحقول يحتاج
   Property Setter أو Custom Field أو ترحيل صريح.
4. **يوجد منطق حيّ خارج المستودع** (Server Scripts يدوية) — انظر القسم ٤.
5. **لا تُنفَّذ بنود المرحلة ب أثناء المرحلة أ.**

---

## ٨. صفحة الهبوط بعد الدخول (2026-07-22)

**العَرَض:** بعد الدخول تظهر شبكة وحدات ERPNext عامة — لا اللوحات السبع.

**التشخيص:** الفحص المباشر على الخادم أثبت أن إعداد اللوحات **سليم وناجٍ من
إعادة النشر**:

```
default_workspace = Home          (Administrator + adelabdulkrime@gmail.com)
seq 1..7 = Home, General Settings, Accounting, Inventory, Purchasing, Sales, Workshop
hidden=1 على 11 وحدة غير ذات صلة
```

فالمشكلة كانت في **نقطة الدخول** لا في التخصيص.

**السبب الجذري:** فحص الترويسات كشف أن **v16 نقل سطح المكتب من `/app` إلى
`/desk`**، و`/desk` المجرّد يفتح مُشغّل الوحدات الجديد الذي يُبنى من `Module Def`
ولا يحترم `default_workspace`. فالشبكة المعروضة ليست صفحة قديمة بل شاشة v16:

```
/          -> 301 /app/home        (بعد الإصلاح)
/app/home  -> 301 /desk/home       (إعادة تسمية v16)
```

**العلاج:** صف `Website Route Redirect` من `/` إلى `/app/home` يُنشئه
[`workshop_landing.py`](khat_workshop/khat_workshop/setup/workshop_landing.py)
في كل نشر. استُهدف `/app/home` لا `/desk/home` ليعمل على النسختين.

**محاولة فاشلة سُجِّلت عمدًا:** ضبط `Website Settings.home_page = "app"` **كسر
الجذر بـ 404 للجميع**. الحقل يقبل *مسار موقع* فقط، و`app` ليس كذلك. تُراجِع خلال
دقائق ووُثِّق في الشيفرة نفسها منعًا للتكرار.

**درسان:** غياب التخصيص عن الشاشة لا يعني غيابه عن النظام؛ وأي تغيير يمسّ
التوجيه يُختبر بـ `curl` على الحالة الفعلية قبل إعلان نجاحه.

---

## ٩. تنبيهات نفاد المخزون (2026-07-22)

**الحاجة:** كانت الورشة تكتشف نفاد القطعة حين يطلبها الفني — فتتوقف السيارة
ويُشترى بالتجزئة. ERPNext يملك الآلية أصلًا؛ كانت معطّلة.

**المُنفَّذ** — [`workshop_stock_alerts.py`](khat_workshop/khat_workshop/setup/workshop_stock_alerts.py):

- `Stock Settings.auto_indent = 1`
- حدّ إعادة طلب على **١٨ قطعة**، مشتقّ من الرصيد الافتتاحي (٣٠٪ تغطية، بحدٍّ
  أدنى ٢) فلا ينفصل الملف عن الكتالوج
- التحقق من المجدول: `enable_scheduler` في القاعدة، و`pause_scheduler` /
  `disable_scheduler` / `maintenance_mode` في إعدادات الموقع تُفحص وتُعلَن لأن
  تعديلها ليس بيد الكود

**إثبات فعلي لا افتراض:**

```
job_type: Daily Maintenance, stopped=0, last_execution=2026-07-22 00:00:28
SP-RAD: رصيد 5 ← رُفع الحدّ مؤقتًا إلى 6
reorder_item() → MAT-MR-2026-00001  (SP-RAD, qty=5, Stores - KAJ)
أُعيد الحدّ إلى 2 وحُذف مستند الاختبار
```

**قيد معلوم:** `reorder_email_notify = 0` لعدم وجود حساب بريد صادر. التنبيه يظهر
كطلب مواد داخل النظام، ولا يصل بريدًا حتى يُضبط SMTP. مُفعّل تلقائيًا في الكود
متى وُجد الحساب.

**خطأ سُجِّل:** `pause_scheduler` ليس حقلًا في `System Settings` بنسخة ١٦ —
الصحيح `enable_scheduler`. أوقف التنفيذ في أول تشغيل قبل إنشاء أي صف.

---

## ١٠. زمن العمالة وربحية البطاقة (2026-07-22)

**الفجوة:** كانت الورشة تعرف إيرادها فقط. لا رقم في النظام يجيب «هل ربحنا في هذه
البطاقة؟» — فالتسعير تقدير، ولا يمكن مقارنة قسم بقسم.

**البنية** — [`costing.py`](khat_workshop/khat_workshop/costing.py) عبر `doc_events`
لا Server Script، لأن الحساب يقرأ تقييم الأصناف وحركة المخزون ويشترك بين الحفظ
والترحيل:

```
التكلفة = (ساعات × تكلفة الساعة) + العمولات + تقييم القطع المصروفة الفعلي
```

**تقييم القطع فعلي لا سعر بيع.** يُقرأ من `Stock Entry.total_outgoing_value`
متى وُجد، ويُقدَّر من `valuation_rate` قبل الصرف. هذا هو الفرق بين هامش حقيقي
وهامش وهمي.

**الحقول** — [`workshop_labour_costing.py`](khat_workshop/khat_workshop/setup/workshop_labour_costing.py):
`hourly_cost` على الفني، و`hours`/`hourly_cost`/`labour_cost` على سطر الفني،
وكتلة «التكلفة والربحية» على البطاقة. كلها `allow_on_submit` لأن تكلفة القطع
لا تُعرف إلا **بعد** الترحيل حين تُنشأ حركة المخزون.

**إثبات فعلي:**

```
بطاقة: خدمة 45 + قطعتان 40 = إيراد 85.000
عمالة  3 ساعات × 2.750 = 8.250   عمولة 2.000
قطع    30.000  ← مطابق تمامًا لـ MAT-STE-2026-00022
تكلفة  40.250   ربح 44.750   هامش 52.65%
كل الفحوص السبعة PASS ؛ حُذفت بطاقة الاختبار وعاد الرصيد إلى 18
```

**اكتشاف جانبي مهم:** `grand_total` على بطاقة العمل **يُحسب في المتصفح فقط**.
أي بطاقة تُنشأ عبر API أو سكربت تصل بإيراد فارغ — وكان الهامش سيُقرأ كخسارة
كاملة بصمت. أضيف احتياطي يشتق الإيراد من السطور ويُصلح `grand_total`.

**قيد:** أسعار الساعة تقديرية حسب التخصص (٢٫٥٠٠–٣٫٠٠٠ ر.ع) وتحتاج مراجعة المالك.

---

## ١١. سجل حالة المركبة عند الاستلام (2026-07-23)

**التعرّض:** العميل يستلم سيارته، يشير إلى خدش، ويقول إنه حدث عندكم. بلا سجل،
الورشة إمّا تدفع أو تتخاصم — ولورشة سمكرة وحوادث هذا الخصام يتكرر.

**المُنفَّذ** — [`workshop_vehicle_intake.py`](khat_workshop/khat_workshop/setup/workshop_vehicle_intake.py):

- `Work Card Damage` — **١٦ منطقة** بترتيب الدوران حول السيارة، و**٦ حالات**
  (خدش، انبعاج، كسر، صدأ، مفقود، دهان سابق)، ولكل سطر صورة
- `Work Card Intake Photo` — صور الاستلام
- مستوى الوقود، وملاحظات، و**توقيع العميل** (حقل Signature)

**غير قابل للتعديل بعد الترحيل — عمدًا.** كل ما سبق جعلناه `allow_on_submit`
لأن العمل يتطور؛ هذا وحده لا. سجل حالة يمكن تغييره لاحقًا لا يساوي شيئًا كدليل،
وهو سبب وجوده الوحيد.

**الإلزام** — [`intake.py`](khat_workshop/khat_workshop/intake.py) على `before_submit`:
لا تُرحَّل بطاقة قبل تسجيل الحالة. المخرج بنقرة واحدة: «لا توجد أضرار ظاهرة» —
فالسيارة النظيفة لا تكلّف أحدًا وقتًا، لكن العبارة صارت **إقرارًا من شخص معروف
في وقت معروف** بدل حقل فارغ لا يثبت شيئًا.

**الاختبار** — [`intake_test.py`](khat_workshop/khat_workshop/intake_test.py) — ٦/٦:

```
[PASS] الترحيل مرفوض بلا تسجيل حالة
[PASS] «لا توجد أضرار ظاهرة» يسمح بالترحيل
[PASS] أسطر الأضرار تسمح بالترحيل وتبقى بعده
[PASS] المنطقة مسجّلة بدقة
[PASS] السجل مجمّد بعد الترحيل
```

**متبقٍّ:** نموذج طباعة يوقّعه العميل ورقيًا. التوقيع الرقمي مسجَّل الآن، والنموذج
المطبوع عمل مستقل.

---

## ١٢. لغة الموقع (2026-07-23)

**البلاغ:** «اللغة لم تعد تظهر كما كانت.»

**التحقق من سجل التغييرات أثبت أنني لم أمسّها:**

```
2026-07-20 20:08  Administrator  language: '' → 'English'   (الإنشاء الأول)
2026-07-20 21:47  Administrator  country: Yemen → Oman      (اللغة لم تُصحَّح)
2026-07-21 03:00  المالك         لا تغيير في اللغة
```

**السبب:** لغة الموقع إنجليزية منذ الإنشاء. حساب المالك يحمل `ar` شخصيًا،
و`Administrator` بلا لغة فيرث الإنجليزية — فالنظام نفسه يظهر بلغتين حسب من يفتحه.

**تصحيح لفهمي:** التسميات الأصلية في هذا النظام **عربية**، و
[`workshop_translations.py`](khat_workshop/khat_workshop/setup/workshop_translations.py)
يسجّل مقابلها **الإنجليزي** تحت اللغة `en`. أي أن الإنجليزية التي ظهرت كانت
الترجمة تعمل كما صُمّمت. لولا قراءة الملف لعكستُ الاتجاه و«أصلحتُ» ما ليس معطلًا.

**العلاج** — [`workshop_language.py`](khat_workshop/khat_workshop/setup/workshop_language.py):
`System Settings.language = ar` و`default lang = ar`، وتعبئة لغة المستخدمين
**الفارغة فقط** — من اختار لغة يبقى على اختياره.

```
LANGUAGE target=ar changed=system_settings,default_lang users_filled=1
Administrator = 'ar'   المالك = 'ar'
```

---

## ١٣. مبدّل اللغة (2026-07-23)

**السبب:** [`desk.js`](khat_workshop/khat_workshop/public/js/desk.js) كان يحقن المبدّل
في `.page-icon-group` — عنصر من واجهة النسخة ١٥. النسخة ١٦ استبدلت الشريط العلوي
بشريط جانبي، فالعنصر غير موجود و**السكربت كان يخرج عند سطره الثاني**. لم يكن معطلًا
بصوت، بل صامتًا تمامًا — النمط نفسه الذي كلّفنا مرارًا.

**العلاج** — [`workshop_navbar.py`](khat_workshop/khat_workshop/setup/workshop_navbar.py):
عنصر في `Navbar Settings` بدل حقن DOM. سجل في قاعدة البيانات لا محدّد CSS، فلا
يتقادم مع أي تغيير قادم في الواجهة.

زر واحد يعرض اللغة **الأخرى** دائمًا، فلا يحتاج منطق شروط.

**قيد:** حقل `Navbar Item.action` محدود بـ **١٤٠ حرفًا** — أوقف المحاولة الأولى،
واستدعى صيغة مختصرة غير مقروءة.

**إضافة:** أُخفي عنصر **Delete Demo Data** من نفس القائمة — زر بهذا الاسم أمام
العميل مربك في أحسن الأحوال. أُخفي ولم يُحذف لأنه عنصر ERPNext قياسي يعيده `migrate`.

**`desk.js`** أُفرغ مع توثيق السبب: ملف محمّل بلا سلوك خير من كود ميت يوحي بأنه يعمل.
